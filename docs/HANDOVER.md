# HANDOVER.md — pdfjig

Claude Code への引き継ぎ文書。
前提知識は `SPEC.md`、遵守事項は `CLAUDE.md` にある。**両方を読んでから着手すること。**

現状: Phase 0 〜 Phase 4 まで完了し、**2026-08-23 に v0.1.0 を公開した。**
`pdf-core` は基盤型・テキスト抽出・ページ操作・描画、
`pdf-desktop` はサムネイル一覧、ドラッグ&ドロップ並べ替え、削除・回転・範囲指定、
結合・分割、パスワード入力までが動き、jpackage で MSI / EXE / ZIP を作れる。

- https://github.com/propagandist/pdfjig/releases/tag/v0.1.0

**残りは GitHub Issues のマイルストーンが持つ**（`CLAUDE.md`「作業の型」。
**ここに書かない理由は org `work-conventions.md` §2**）。

- https://github.com/propagandist/pdfjig/milestones

---

## 確定している判断

議論を経て決定済み。再検討は不要。

| 項目 | 決定 | 理由 |
|---|---|---|
| スタック | Java 21 + JavaFX + PDFBox 3 | 表抽出の成熟実装が Java にしかない |
| ライセンス | Apache-2.0 | iText (AGPL) を排除する制約でもある |
| Excel 出力 | M1 に含める | CSV は先頭ゼロ消失・日付自動変換で実務データを壊す |
| M0 の UI | サムネイル並べ替えまで実装 | リスト表示だけなら既存 CLI に対する優位性がない |
| Maven Central | 初版では使わない | アプリケーションでありライブラリではない |
| 配布 | GitHub Releases + jpackage | MSI / EXE |
| 表示名 | `PDFjig` | コマンド名・パッケージ名は `pdfjig` のまま。コマンドは小文字が自然 |
| ベンダー名 | `PROPAGANDIST CORPORATION` | MSI の発行元 |
| 配布形式 | MSI + EXE + ZIP | 形式ごとに役割を分ける（下記 4-1） |
| 最小 Windows | 10 21H2 (x64) | サポート終了済みの古いビルドを切る |
| JavaFX ランタイム | Gluon の jmods を Gradle で取得 | JavaFX 同梱 JDK を全員に強いない |
| MSI UpgradeCode | `3210BCE4-3635-4EFC-8EC1-DC77881091BB` | **二度と変えない。** 変えると旧版が残る |
| ブランチ | `develop` 単線。`main` はリリース時に作る | 下記「ブランチをどう使っているか」 |
| 整形 | Spotless（palantir / ktlint） | 下記「整形を Spotless に寄せた」 |

---

## Phase 0: 初期コミット

コードより先に、以下を置く。空リポジトリの段階で方針を固定するのが目的。

### 0-1. LICENSE

Apache License 2.0 の全文。

### 0-2. README.md

最低限、以下を含める。

- 1 行の説明: 「PDF を綴じ、解き、取り出すためのデスクトップユーティリティ」
- 名前の由来（治具 — 同じ作業を同じ精度で繰り返すための道具）
- 設計思想 4 点（`SPEC.md` §1）
- **Non-goals セクション** — テキスト直接編集 / 電子署名 / フォーム作成 / 帳票生成 / 全文 RAG チャット

Non-goals を最初から書いておくことで、後から要望が来たときに「方針変更」ではなく「当初からの設計」として説明できる。

### 0-3. .gitignore

Java / Gradle 用に加えて、以下を必ず含める。

```gitignore
*.pdf
!src/test/resources/**/*.pdf

*.key
.env
credentials.*
```

実業務の PDF が Public リポジトリに混入すると取り返しがつかない（`CLAUDE.md` INV-6）。

### 0-4. SECURITY.md

パスワード機能を持つ以上、脆弱性報告の窓口は最初から必要。
報告先メールアドレスと、対応方針（受領確認までの目安日数）を記載する。

### 0-5. SPEC.md / CLAUDE.md

本パッケージのファイルをそのまま配置する。

---

## Phase 1: プロジェクト骨格

### 1-1. Gradle マルチモジュール構成

```
pdfjig/
├── settings.gradle.kts
├── build.gradle.kts
├── gradle/libs.versions.toml    バージョンカタログ
├── pdf-core/build.gradle.kts
├── pdf-ai/build.gradle.kts
├── pdf-cli/build.gradle.kts
└── pdf-desktop/build.gradle.kts
```

依存宣言:

- `pdf-core` — PDFBox, tabula-java, POI のみ。**他モジュールに依存しない**
- `pdf-ai` — `pdf-core`
- `pdf-cli` — `pdf-core`, `pdf-ai`, picocli
- `pdf-desktop` — `pdf-core`, `pdf-ai`, JavaFX

### 1-2. ArchUnit テスト

最初に書くテストがこれ。INV-1 を機械的に守るため。

```java
@Test
void coreMustNotDependOnAi() {
    noClasses().that().resideInAPackage("..pdfjig.core..")
        .should().dependOnClassesThat().resideInAPackage("..pdfjig.ai..")
        .check(new ClassFileImporter().importPackages("com.propagandist.pdfjig"));
}
```

あわせて「`pdf-core` 外から PDFBox の型が見えないこと」も検証する。

### 1-3. CI

GitHub Actions。`push` / `pull_request` で `./gradlew build` を回す。
リリースタグでの jpackage ビルドは Phase 4 で追加する。

画面を操作するテスト（`:pdf-desktop:uiTest`）は `build` に含めず、windows ジョブから
別に呼んでいる。デスクトップセッションを要するため（下の「UI テストの自動化」）。

---

## Phase 2: pdf-core（M0 範囲）

依存のないところから積む。

### 2-1. 基盤型

`PdfDocument`（AutoCloseable）、`PdfjigException`、`ErrorCode`、`PageRange`、`Rotation`。

`PdfDocument.open(Path, char[])` の時点で INV-5 が関わる。
パスワードはコンストラクタで使用後に即ゼロ埋めし、フィールドに保持しない。

### 2-2. TextExtraction

`extractAll` / `extractByPage` / `extractWithPositions`。
PDFBox の `PDFTextStripper` を使う。座標付き抽出は `PDFTextStripper` を継承して
`writeString(String, List<TextPosition>)` をオーバーライドする。

### 2-3. PageOperations

`merge` / `split` / `reorder` / `rotate` / `extractPages` / `deletePages`。

**ここで暗号化の伝播を必ず実装する**（`SPEC.md` §4.3）。
M0 では暗号化「設定」機能はないが、暗号化された入力を受け取る可能性はある。
`EncryptionPropagation` の判定と警告発出は M0 の時点で入れておく。
後から足すと、既に平文出力してしまった利用者が発生する。

### 2-4. テスト

各操作について、ページ数・ページ順・回転角が期待通りかを検証する。
フィクスチャ PDF は PDFBox で生成するユーティリティをテストコードに置き、リポジトリには
生成済みファイルを最小限だけ置く。

---

## Phase 3: pdf-desktop（M0 範囲）

### 3-1. サムネイル一覧

要求: 固定サイズ、遅延レンダリング、LRU キャッシュ。

実装上の要点:

- `PDFRenderer#renderImageWithDPI` は必ず `Task` の中で呼ぶ（`CLAUDE.md` JavaFX 節）
- 可視範囲外のページはレンダリングしない。`ListView` / `GridView` の cell factory で
  cell が表示された時点でレンダリングを開始する
- キャッシュサイズは上限ページ数で管理する（例: 直近 200 ページ）。
  メモリ量ではなく枚数で切るほうが挙動が読みやすい
- レンダリング中はプレースホルダを表示し、UI をブロックしない

### 3-2. ドラッグ&ドロップ並べ替え

単一文書内のみ。文書間のページ移動は実装しない。

並べ替えは UI 上の順序リストを更新するだけで、この時点ではファイルを書き換えない。
「保存」操作で `PageOperations.reorder` を呼ぶ。

### 3-3. 基本操作の UI

結合（複数ファイル選択）、分割、ページ削除、回転、範囲抽出。
それぞれ確定的処理を直接呼ぶ。承認フローは不要（AI が関わらないため）。

メニューに加えて、繰り返し使うものはツールバーにも出す（Phase 4 で追加）。

### 3-4. パスワード入力ダイアログ

暗号化された PDF を開こうとした場合に表示する。
`PasswordField` を使い、`char[]` で受け渡す。入力値を `String` に変換しない。

M0 では「開く」だけ。設定・解除は M1。

---

## Phase 4: リリース

### 4-1. jpackage（実装済み）

`installDist` → `jlink` → `jpackage` の 3 段。`./gradlew :pdf-desktop:packageAll -Pversion=0.1.0`
で `dist/` に 3 つの成果物ができる。手元で回すには WiX 3.14 を PATH に通す必要がある
（jpackage が要求するのは 3.x で、v4 では動かない）。

配布形式は役割で分けた。同じものを 2 つ並べるのではなく、

- **EXE** — ユーザー単位。管理者権限が要らない。個人向け
- **MSI** — マシン単位。`--win-dir-chooser` で入れ先を選べる。情報システム部門による一括配布向け
- **ZIP** — インストール不要。インストーラ自体が使えない環境向け

EXE と MSI は同じ製品であり、両方を同時には入れられない。jpackage は ProductCode を
名前とバージョンから決めており、両者で同一になるため。二重に入るよりは入らないほうが説明できる。

実測サイズは各 60MB 前後（ランタイムイメージ 59MB、アプリイメージ 82MB）。
→ **2026-08-22 訂正: POI を外して各 45MB 前後になった**（MSI 45.2 / EXE 45.8 / ZIP 44.1 MB。
アプリイメージ 63MB。ランタイムイメージは 59MB で変わらない——外した 2 モジュールは小さい）。
M1 で POI を戻せば元の水準へ戻る。

### 4-2. GitHub Actions によるリリース自動化（実装済み）

`.github/workflows/release.yml`。タグ `v*` の push、または `workflow_dispatch` で走る。
`runs-on` は `windows-2022` に固定し、WiX もランナーイメージのプリインストールに頼らず
ワークフロー内で用意する。イメージ更新で黙って壊れるのを避けるため。

**draft リリースとして作る。** 実機で触ってから公開する。

### 4-3. リリースの段取り

**版に依らない手順である。** その版にだけかかるもの（着手の順序・捨てタグの形・
同時に公開するもの）は「vX.Y.Z をリリースする」issue が持つ（`CLAUDE.md`「マイルストーン」）。
**ここへ写さない。**

**ここから先は人が行う。**

1. **実機確認**（下の「リリース前の確認」）。**捨てタグの成果物で行う**——
   タグを打ち直す前に、配るものと同じバイト列を触っておく
2. **`docs/RELEASE_NOTES.md` をその版のものにする。**
   ★ **これは `release.yml` の `body_path` であり、リリース本文そのものである。**
   前の版のまま打つと、本文と中身が食い違ったものが付く。
   **版数・成果物のファイル名・「この版で直したこと」を見る**
3. **`README.md` の最新版と、前の版に付けた断りを見直す。**
   直した欠陥の断りが残っていると、**直っているのに壊れていると読ませる**
4. **状態を書いた記述が腐っていないか見る**（`SECURITY.md` の「サポート対象バージョン」など）。
   ── **公開した瞬間に嘘になるものは、タグを打つ前に直す。**
   タグと、そこから作る `main` が指すコミットに「まだ出ていない」を残さないため
5. **捨てタグでワークフローを一度通す**（下の「捨てタグ」）
6. `git tag vX.Y.Z && git push origin vX.Y.Z`
7. Actions が draft リリースを作るので、**成果物を触ってから公開する**
8. **同時に公開するものがあれば、公開と同じ場で出す**（Security Advisory など。
   `SECURITY.md`「修正版の届き方」）
9. **`main` をその版まで進める**
10. **その版のマイルストーンを閉じ、「vX.Y.Z をリリースする」issue を閉じる**
    ── **閉じる条件はマイルストーンの description が持つ。ここへ写さない。**
    ★ **閉じる前に、残っている open を見る。** 閉じる条件は「**中身 ＋ 公開**」の連言で
    ある（`CLAUDE.md`「マイルストーン」）。**公開しただけでは片方しか満たしていない。**
    - **その版の中身にあたるものが残っているなら、まだ閉じない。**
      **配る中身が揃っていない版を配ったということ**であり、直すのは description か中身の側で、
      マイルストーンの状態ではない
    - **中身にあたらないものは次の版へ移す。次に配られる版に乗るからである**
      ── ★ **次の版のマイルストーンが無ければ、ここで作ってよい。**
      **残りがあるということは、その版は着手が見えている**（`CLAUDE.md`「マイルストーン」の
      「着手が見えていない版は作らない」に反しない）
    ★ **移すのは開いているものだけである。** 閉じた issue のマイルストーンは動かさない。
    v0.1.1 は 0 open で閉じており、先例もそうである。
    ★ **issue からマイルストーンは外さない**（`CLAUDE.md`「マイルストーン」）。
    閉じるのはマイルストーンの状態だけである。
    ★ **`gh issue list --milestone` は PR を除くので、マイルストーンの
    `closed_issues` と数が合わないことがある。** 食い違いを「issue が外れた」と
    読まないこと（v0.1.1 は 11 issue ＋ PR 1 本で 12 だった。→ **2026-08-27 訂正:
    #42 / #70 を付け替えたので、いまは 13 issue ＋ PR 1 本で 14 である**。#83。
    **食い違いが出るという話そのものは変わらない**）

### 4-4. リリース前の確認

**この節は `security-baseline.md` §5.3.2 が求める「配る前に確認する手順」である。**
org が求めるのは手順があることだけで、**中身はこちらが持つ**（同 §5.3 の★）。**写さない。**

**「CI は GUI を起動できない」という前提は 2026-08-22 に無くなった**
（下の「UI テストの自動化」）。いま自動で見ている範囲と、人が見る範囲を分けてある。

**この 2 つを混ぜないこと。** CI が緑であることを実態より広く読むと、
確認したつもりの穴がそのまま配られる。

#### 配る範囲が門を通っているか

★ **ここだけ軸が違う。下の 2 つは成果物を見るが、ここは差分を見る**——
門そのものは `CLAUDE.md`「配る差分の門」が持つ。**中身をここへ写さない。**

```bash
git log --oneline <前のタグ>..HEAD
```

これと、その版でマージした PR 本文の `門:` の範囲を突き合わせる。
**差が無ければ 0 分。** 差があるコミットだけ門へ通す。

★ **直接 `develop` へ押したコミットはここで出る。門を通さずに配られる唯一の経路である。**

#### CI が見るもの

| いつ | 何を |
|---|---|
| `build`（push / PR、windows のみ） | 開く・回す・消す・並べ替える・区切って分割する・保存する。パスワード付きの文書を開く（`:pdf-desktop:uiTest`） |
| `release`（タグ） | ZIP を展開して `PDFjig.exe` が起動し、ツールバーが組み上がるところまで |

★ **手元で uiTest を走らせるときは `tools/sandbox/Invoke-UiTestInSandbox.ps1` を使える。**
ホストの `build/` と `~/.gradle` を汚さず、画面をロックしたままでも走る。
**ただしカーソルは奪われる**——理由と実測は下の「uiTest を手元で隔離しようとして、できないと分かった」。

#### 人が見るもの

1. **ZIP を展開し `PDFjig.exe` を直接起動** → 管理者権限なしで起動する
   ── CI も見ているが、実機でも一度は触ること。ランナーは検証用の綺麗な Windows であり、
   利用者の環境ではない
2. **EXE を実行** → 管理者昇格を求められずに入る。スタートメニューに `PDFjig` が出る
   ── ★ **2〜5 番は `tools/sandbox/Invoke-InstallCheckInSandbox.ps1` が機械で見る**
   （下の「uiTest を手元で隔離しようとして、できないと分かった」に実測がある）。
   **人が残す必要があるのは「標準ユーザーの環境で入るか」だけ**——Sandbox の既定ユーザーは
   管理者であり、そこは確かめられない。**CI では守られていない**（起票済み）
3. **MSI を実行** → インストール先を選べる。`%ProgramFiles%` に入る（同上）
4. **アンインストールでファイルが残らない**（同上）
5. **同じ MSI をもう一度入れて上書きになる**（UpgradeCode の確認。同上）
6. **ネットワークを遮断したまま**一連の操作が動く（INV-3 の確認）
   ── ランナーで遮断すると Gradle も止まる。何が原因で落ちたのか切り分けられない
   ── ★ **`tools/sandbox/Invoke-UiTestInSandbox.ps1` は `<Networking>Disable</Networking>` で
   29 本を通している**（2026-08-23 実測。依存は読み取り専用のキャッシュから引くので
   Gradle は止まらない）。**それでも人手から消さない**——Sandbox は綺麗な Windows であって
   利用者の環境ではない。1 番と同じ理由である
7. **日本語のファイル名・パスの PDF を開ける**
   ── ランナーのロケールは利用環境と違う。日本語環境で見るほうが確か
8. **表示倍率 150% でサムネイルが眠くない**
   ── 目で見る以外にない
9. **ドラッグでの並べ替え**
   ── **★ ここは CI で守られていない。** `ReorderUiTest` は手元では走るが、
   windows ランナーでは 1 本で 19 分かかるため CI から外してある（上の「UI テストの自動化」）。
   **動くこと自体を人が確かめる必要がある。** 掴んだ感じが自然かどうかも同じ場で見る。
   ── なお Windows Sandbox では 3.4 秒で通る（2026-08-23 実測）。**掴んだ感じは見られない**が、
   壊れていないことだけなら `Invoke-UiTestInSandbox.ps1` で確かめられる
10. **既存のファイルを選んで「名前を付けて保存」** → Windows の保存ダイアログが
    「置き換えますか？」と訊き、「はい」で置き換わる。「いいえ」なら何も起きない
    ── **★ ここは CI で守られていない。** 確認を出すのは OS のダイアログであり、
    `FileDialogs` の向こう側は自動テストの対象にできない（`CLAUDE.md`「JavaFX」）。
    さらに **uiTest は既存ファイルへの保存を一度も通っていない**——`saveAs` の呼び出し元 5 つは
    すべて `@TempDir` の下の新規パスを渡す。**`DesktopUiTest#saveAs` は
    `Files.exists(output) && size > 0` で完了を待つので、既にあるファイルを渡すと
    書き出す前に条件が満たされ、待てなくなる。** 9 番と同じ理由で人が見る側に置いてある。
    **上書きするのが正しい挙動である**——`pdf-core` は拒むが、画面の保存は確認を取ってから
    置き換える。約束の違いは `SPEC.md` §4.2 に書いてある（#51）
11. **3 ファイルを開き、スクロールでサムネイルを描かせながら「×」で 1 つ外す。
    ★ 外す位置を変えて 2 回見る**（#52 / #88）
    ── **1 つ目を外す** → 残ったページの絵が入れ替わらない。**2 ファイルでは再現しない**——
    繰り下がった先の鍵を引く相手が居ないためである
    ── **最後（または真ん中）を外す** → 絵の出ないタイルが残らない。
    **1 つ目では再現しない**——先頭を外すと残り全部の出どころ番号が繰り下がるため、
    一覧の全行が別物になって描き直される。**前を残して外したときだけ、中身の変わらなかった行が
    セル更新を受けず**、捨てられた描画のタイルが空のまま残りうる
    ── **★ どちらも CI で守られていない。** 供給する側の待ち合わせは
    `ThumbnailSourceUiTest` が見るが、**捨てられた描画をタイルが頼み直す側**
    （`ThumbnailTile#setOnCancelled`）は `ThumbnailGrid` と `ListView` のセル更新をまたぐため、
    **時間に依存しない形にできなかった**
12. **設定が再起動をまたぐ**（#12）。**開く → 保存 → 閉じる → もう一度起動**して、
    **「開く」も「名前を付けて保存」も前回のフォルダから始まる**こと。
    ★ **読む用と書く用は別々である。**PDF を取ってきた場所と書き出した場所が違うときに、
    それぞれ正しい側から始まるかを見る
    ── **★★ ここは CI でも uiTest でも守られていない。** `Settings` と `RecentFolders` の
    単体テストはあるが、**配線（起動で復元 → 終了で保存）を通るテストが 1 本も無い。**
    `uiTest` は `MainWindow` を直に組むので `PdfjigApplication` を迂回し、
    **迂回しているのは意図した設計である**——そうでないと**テストが開発機の
    `%LOCALAPPDATA%` を書き換える**（`docs/SPEC.md` §10）
    ── ★ **ファイルの関連付けから開いた直後も見る**（#108 で踏んだ）。復元は背景で走るので、
    **その間に開いた文書の隣を、前回のぶんで塗り潰していないこと。**
    「開く」がいま開いている文書のフォルダから始まれば正しい
    ── あわせて 3 つ見る。**`%LOCALAPPDATA%\pdfjig\settings.properties` が実際にでき、
    中身がコメント行（見出しと日付）を除いて 2 行だけである**こと
    （**パスワードも文書のパスも無い**）／**まだファイルが無い状態で何も開かずに閉じたら、
    ファイルができない**こと（★ **一度できた後は前回のぶんが書き戻されるので、この確認は
    最初の 1 回しかできない**）／**ファイルを消しても、次の起動が既定に戻るだけ**であること
    ── ★ **切れたネットワーク共有を覚えた状態でも起動が固まらない**こと。
    存在の確認は背景スレッドへ追い出してあるが、**そこは実機でしか確かめられない**
    ── ★★ **追い出したのは起動のときだけである。** ダイアログを開くときの確認
    （`RecentFolders#reading()`）は**いまも JavaFX スレッドで走る**ので、
    **セッションの途中で共有が切れれば固まる。** #107 より前からそうであり、
    **この差分で悪くなってはいないが、直ってもいない**

13. **ログが実機で書かれる**（#13）。**正常に一巡しても `%LOCALAPPDATA%\pdfjig\logs\` が
    できない**こと。次に `settings.properties` を読み取り専用にして起動 → フォルダを選ぶ →
    閉じる、で **`pdfjig.0.log` ができ、`SETTINGS_UNWRITABLE` の行が入る**こと
    ── **開いて中身を見る。パスもファイル名も、例外のメッセージも入っていない**こと。
    見出しに「何を書いていないか」が載っていること
    ── **★ ここは CI で守られていない。** `LogsTest` が書き方（メッセージを落とす・世代を回す・
    `%` を逃がす）を縛り、ArchUnit が口の迂回を禁じているが、**置き場を決めるのは
    `%LOCALAPPDATA%` を読む経路**であり、**CI の ubuntu には存在しない。**
    12 番と同じく `PdfjigApplication` を通る配線であり、**uiTest は `MainWindow` を直に組んで
    そこを迂回する**（迂回は意図した設計。`docs/SPEC.md` §10）
    ── ★★ **配布物（ZIP か EXE）で見ること。** `./gradlew run` には標準エラーがあるので、
    **ログが唯一の手がかりである状態を再現できない**——`--win-console` を付けていないのが
    この機能の理由そのものである

#### 捨てタグ

タグを打つ前に捨てタグでワークフローを一度通しておくと、CI 側の失敗を本番のタグで
踏まずに済む。確認後にタグとドラフトを消す（`gh release delete v0.0.4 --cleanup-tag`）。

**実機確認はこの成果物で行う**（4-3 の 1 番）。**配るものと同じバイト列である。**

★ **捨てタグは `v0.0.3` のような数値のみの形にする。** `v0.1.0-rc1` は使えない——
`release.yml` の「バージョンを決める」が `^\d+\.\d+\.\d+$` で弾く（MSI の ProductVersion が
数値しか受け付けないため）。**通したいワークフローの入口で落ちるので、何も確かめられない。**

★ `workflow_dispatch` は代わりにならない。**タグ名からバージョンを切り出す経路
（`github.ref_name` の `-replace '^v', ''`）を通らない**ので、そこだけが本番で初めて走る。

---

## M1 以降の概要

**期の定義は `SPEC.md` §8 が持ち、やることは GitHub Issues のマイルストーンが持つ**
（`CLAUDE.md`「作業の型」）。**ここに一覧を置かない**（org `work-conventions.md` §2）。

- https://github.com/propagandist/pdfjig/milestones

M1 で `AiProvider` を導入する際、実装順は `NoOpProvider` → `OllamaProvider` → `AnthropicProvider`。
NoOp を最初に作ることで INV-3 が構造的に守られる。

---

## 実装中に決めた判断

仕様に書かれておらず、実装を進めるために決めた事項。いずれも方針が違えば差し戻せる。

### テキスト抽出（Phase 2-2）

- [ ] 抽出時に座標順への並べ替え（`setSortByPosition(true)`）を既定にした。
      コンテンツストリーム順のままだと多段組の文書で読み順が崩れ、`pdf-ai` に渡す
      テキストの品質に直結するため
- [ ] 改行を LF に固定した。`System.lineSeparator()` に従うと、同じ入力から
      環境ごとに異なる出力が出る
- [ ] `PositionedText` の座標系をページ左上原点とした（PDF 本来は左下原点）。
      画面表示と向きを揃えるため。tabula-java は PDF 座標系を使うので、
      M1 の表抽出では変換が要る

### ページ操作（Phase 2-3）

- [ ] 出力先に同名のファイルがある場合は必ず失敗させる（`OUTPUT_ALREADY_EXISTS`）。
      上書きするかどうかは利用者の判断であり、確認は UI / CLI の責務とする。
      入力と同じパスを出力に指定して入力を壊す事故もこれで同時に防がれる
- [ ] `rotate` の指定は現在の回転角への加算とした。UI の「右に回す」という操作と
      一致させるため。絶対角の指定にはしていない
- [ ] 分割の出力ファイル名を `<入力名>_001.pdf` とした
- [ ] `EncryptionPropagation.INHERIT` / `PROMPT` は M0 では未対応とし、指定されたら
      失敗させる。引き継ぎには元のパスワードが必要であり、パスワードを受け取る経路を
      持つ M1 の暗号化機能とあわせて実装する
- [ ] パスワードが必要な入力に対するページ操作は M0 では行えない。同じく M1 で対応する
- [ ] `SPEC.md` §4.2 にない `PageOperations.assemble(Path, List<PageSelection>, Path)` と
      値型 `PageSelection` を追加した。UI で並べ替え・削除・回転を続けて行った結果は、
      一度の保存で確定させないと「並べ替えた状態で回転したら並べ替えが消えた文書が
      出てくる」ことになる。個別操作のままでは利用者を誤解させるため
      （`CLAUDE.md` 優先順位 2）。既存の `reorder` / `rotate` / `extractPages` /
      `deletePages` はそのまま残してある。
      → **承認され、`SPEC.md` §4.2 に追記済み**（Phase 4）
- [x] 上に伴い、UI では回転も編集セッションに含めた。`HANDOVER.md` 3-3 は回転を
      独立した操作として挙げているが、サムネイル一覧の上で回して保存するほうが
      並べ替え・削除と揃う。独立操作に戻すべきなら指示がほしい

### UI（Phase 3）

- [x] サムネイル一覧は縦 1 列の `ListView` にした（→ Phase 4 でグリッドに変更）。`GridView` は ControlsFX への依存が
      必要になる。仮想化が効き、セルが表示された時点で描画を始められる点は同じ
- [x] サムネイルの長辺は 160px、キャッシュは 200 枚とした（→ Phase 4 で描画 220px / 表示 150px に変更）
- [ ] 保存は一時ファイルに書いてから置き換える。保存先ダイアログは上書きを確認するが
      `pdf-core` は既存の出力を拒む。先に消すと書き込み失敗で元のファイルが失われる
- [ ] 結合の入力は名前順に並べ、その順序を見せて承認を求める。ファイル選択ダイアログが
      返す順序は環境によって変わり、選んだ順に結合されると思い込ませてしまう
- [ ] UI の分割は編集中の並びを対象にする。`pdf-core` の `split` は元の並びを切るため
      使っていない
- [ ] 起動引数でファイルを開けるようにした。ファイルの関連付けから開く経路であり、
      UI を手で操作せずに動作を確かめる手段でもある
- [x] ~~JavaFX を必要とする部分（サムネイルの供給、ダイアログ）にはユニットテストを
      書いていない。Toolkit の初期化が要り、CI では Monocle などの追加が必要になる~~
      → **2026-08-22 に覆した**（下の「UI テストの自動化」）。Monocle は使わない。
      windows ランナーが対話セッションを持つことを使い、TestFX を headed で回している。
      画面に依存しない `PageOrder` と `LruCache` を先に固めてあったことは今も効いていて、
      画面のテストは「その判断が画面を通したときにも呼ばれるか」だけを見ればよくなっている

### ダイアログを始めるフォルダ（v0.1.0 の実機確認で追加）

- [ ] **`setInitialDirectory` を 4 つのダイアログのどれでも呼んでいなかった。**
      初期フォルダを決めるのは Windows の共通ダイアログ側になり、
      毎回たどり直すことになっていた。JavaFX（`WinCommonDialogs`）は
      `initialDirectory` が未設定なら `null` をネイティブへ素通しするだけで、
      その先はアプリの実行ファイルに紐づく MRU が使われる。前回のフォルダが出ることもあるが
      規則は保証されず、起動のしかたが変われば別の履歴になる
- [ ] **読む用と書く用の 2 つを覚えることにした**（`RecentFolders`）。開く・追加は読む用、
      保存・分割先は書く用。PDF を取ってくる場所と整理した結果を置く場所は違うことが多く、
      片方に引きずられるとそのつどたどり直すことになる。
      まだ使っていない側は、いま開いている文書の隣から始める。どちらも無ければ渡さず、
      今までどおり Windows の既定に任せる。初回にホームへ固定するより馴染みがある
- [ ] **覚えるのは `open(Path)` / `addDocument(Path)` の側に置いた。** ダイアログの側ではなく
      ここに置くことで、**起動引数から開く経路**（ファイルの関連付け）も同じ扱いになる。
      開くのに失敗しても覚える。パスワードが要る文書でも壊れた文書でも、
      次に PDF を探す場所は同じフォルダである
- [ ] **取り出すときに存在を確かめる。** USB を抜く、ネットワークの割り当てが切れる、
      利用者が片づける。無くなったフォルダを渡すとダイアログの出方が読めなくなる
- [ ] `RecentFolders` は JavaFX の型を持たないので**ユニットテストを書いてある**。
      上の「JavaFX を必要とする部分はテストしない」は、Toolkit の初期化が要る部分に限った話であり、
      `Path` だけを扱う判断はテストで固められる
      （その「テストしない」は 2026-08-22 に覆した。この項の判断は変わらない）
- [ ] **アプリを閉じると忘れる。** 再起動をまたいで維持するには設定ファイルが要り、
      その置き場は未決事項として M1 に残してある

### サムネイル一覧（Phase 4 で作り直し）

- [ ] 一覧をグリッドにした。3-1 の縦 1 列という判断を変えている。1 列だと枠が幅いっぱいに
      伸びてページの形が読めず、選択の青い帯が画面を横切る。ページ整理ツールの見た目に
      なっていなかった
- [ ] `GridView`（ControlsFX）は引き続き使わない。**1 セル = 1 行**として `ListView` に
      載せることで、素の JavaFX のまま行単位の仮想化を効かせている。可視範囲外の行は
      セルごと使い回され、サムネイルの描画依頼も出ない
- [ ] 選択は `ListView` の選択モデルではなく `ThumbnailGrid` がページ位置として持つ。
      選ぶのは行ではなく 1 枚のページであり、両者は一致しない。
      矢印キーもページ単位に置き換えてある
- [ ] **修飾キー付きの矢印は `ListView` が自分で握り潰す。** そのままだと回転の Ctrl+→ が
      メニューのアクセラレータまで届かない。フィルタで先に `Scene#getAccelerators` を
      照合して走らせている。キーの割り当て自体はメニュー項目側に一本化したまま
- [ ] サムネイルは長辺 220px で描き、画面には 150px で出す。表示倍率 150% の環境では
      論理 150px が物理 225px になり、等倍で描くと眠い絵になる
- [ ] タイルの外形は全ページ共通で、枠だけが画像の形に貼り付く。桁が揃ったまま
      縦長・横長・回転が見分けられる

### ツールバー（Phase 4 で追加）

- [ ] よく使う操作をツールバーに出した。3-3 はメニューだけを前提にしていたが、
      削除と回転までメニューを開く必要があり、繰り返す作業ほど手数が増える
- [ ] **操作の定義はメニューと共有する。** 文言・ショートカット・有効条件・処理を
      `MainWindow.Action` にまとめ、メニュー項目とボタンの双方をそこから作る。
      別々に書くと片方だけ直したときに挙動がずれる
- [ ] メニューは全部を持ったまま残し、ツールバーには繰り返し使うものだけを置く。
      ツールチップにはメニューと同じ長い文言とショートカットを出す。
      ツールバーの短い表示名だけでは意味が伝わらないため
- [ ] アイコンは JavaFX 標準の `SVGPath` で線として描く（`ToolIcons`）。画像を同梱すると
      表示倍率ごとに用意が要り、アイコンフォントは依存が増える。色と太さは CSS の
      `.tool-icon` に置き、無効時の色は持たない（JavaFX が全体を薄くする）
- [ ] パスごとに外接矩形が違うため、同じ大きさの枠に入れて中央に置く。
      そのまま並べるとボタンごとに図の高さが変わり、下に付く文字の位置が揃わない

### 結合を「PDF を追加」に変えた（Phase 4）

- [ ] **`SPEC.md` §7.1 の「文書間のページ移動」を実装しない欄から外した。** 承認済み。
      当初の結合は開いている文書を一切見ておらず、ファイルを 2 つ以上選ばせて
      その場で書き出す一発勝負のウィザードだった。並べ替え・削除・回転が
      「画面で編集して最後に一度保存」なのに、この操作だけ流れが違い、
      「使い方が分からない」という指摘につながった。綴じる道具としての中心的な操作であり、
      仕様側を改めた。**「複数文書の同時タブ」は実装しないまま残している**
- [ ] 「PDF を追加…」は選んだファイルのページを並びの末尾に足す。差し込む位置は尋ねない。
      足した直後にドラッグで動かせるほうが早い
- [ ] 順序の確認ダイアログは外した。結果がその場でサムネイルに出てドラッグで直せるため、
      確認を挟む理由がなくなった。並べる順序は名前順のまま（選択ダイアログが返す順序は
      環境によって変わり、選んだ順に並ぶと思い込ませてしまう）
- [ ] `PageOperations.merge` は UI からは使わなくなったが残してある。`SPEC.md` §4.2 の
      API であり、M2 の CLI で使う。`assemble` との違いはページ単位で順序と向きを
      指定できるかどうか
- [ ] 「追加」は文書を開いていないと使えない。開いている文書に足す操作であり、
      何も開いていないときは「開く」が担当する
- [ ] 分割ダイアログは枚数しか尋ねておらず、何個のファイルがどんな名前でできるのか
      実行するまで分からなかった。入力に追従して結果を出すようにした
- [ ] **どのページがどのファイルのものか、一目で分かるようにした。** ツールチップだけだと
      1 枚ずつ確かめるしかない。含んでいるファイルの一覧をツールバーの下に出し、
      各ページの下に同じ色の帯を敷く。1 ファイルのときは出さない（表題で足りる）
- [ ] 一覧の枚数は<b>いま並んでいる数</b>を出す。元のページ数ではなく、消したぶんが減る。
      「B から 3 枚だけ残した」という今の状態がそのまま読める
- [ ] 出どころの色は、選択の青とドロップ先の琥珀を避けてある。状態を示す色と
      出どころを示す色が似ていると、どちらの意味か取り違える
- [ ] 帯の当て直しは `ThumbnailTile.show` の早期 return より前で行う。帯の有無は並びの
      中身ではなく含むファイル数で決まり、同じページを出し続けている間に増えることがある。
      あわせて、帯が出るようになった時点で `ListView.refresh` を呼ぶ。
      内容の変わっていないセルは更新されないため

### 分割を「ここで区切る」に変えた（Phase 4）

- [ ] **UI は `EveryNPages`（N ページごとの均等割り）だけを出していた。** 均等割りが当たるのは
      「1 ページずつバラす」か「決まった枚数の綴りが連続している」場合だけで、実務の
      スキャン束は書類ごとに枚数がまちまちなのでまず当たらない。使いどころの狭い切り方だけを
      出していたことが「用途が分からない」につながった
- [ ] サムネイルを見て切れ目に印を付け、その区切りで分ける形にした。`pdf-core` の
      `AtBoundaries` と同じ考え方であり、**M2 の文書境界検出（AI）はこの仕組みに提案を
      流すだけになる**（承認 → 区切りを付ける → 分割）
- [ ] **区切りはページに紐づける**（`PageEntry`）。位置に持たせると、ページを動かしたときに
      「A と B の間で切る」が「A と C の間」に化ける
- [ ] 先頭には区切りを付けられない。先頭は区切らなくてもファイルの始まりである。
      区切りのあるページが先頭へ動いてきた場合も、その区切りは効かないものとして扱う
- [ ] `modified()` は区切りを見ない。書き出す内容が変わらないのに「未保存の変更があります」と
      出るのは筋が違う。`reset()` は区切りも外すため、メニューの文言を
      「並びと向きを元に戻す」→「編集を元に戻す」に変えた
- [ ] 区切りが 1 つも無いときの分割は何もしない。全ページを 1 ファイルに書き出しても
      分割にならず、黙ってそうするより区切りが要ることを伝えるほうが正直である
- [ ] 枚数で機械的に区切る操作は「N ページごとに区切る…」として分けた。区切りを入れる操作と
      書き出す操作を一緒にすると、どこで切られるのか確かめないまま書き出すことになる
- [ ] `PageOperations.split` / `SplitStrategy` は引き続き UI からは使わない。
      `SPEC.md` §4.2 の API であり、M2 の CLI と AI 境界検出で使う
- [ ] 区切りの縦線はタイルに重ねて描く。枠線として持たせると内側の余白が変わり、
      区切りのあるページだけ中身がずれる

### 1 ページずつの分割を独立させた（2026-08-22）

- [ ] **「全部バラす」に辿り着くのに 5 手かかっていた。** 「N ページごとに区切る…」で 1 を入れ、
      画面に戻って「分割」を押す。しかも「全部バラす」という意図から「N ページごとに区切る」を
      選び、N=1 がそれに当たると気づく必要がある。分割のうち最も頻度が高く、最も判断の要らない
      操作にしては、手数も発見性も見合っていなかった
- [ ] **上の「区切りを入れる操作と書き出す操作を分ける」判断は、切り方に判断の余地がある場合の
      ものである。** N=3 で区切ったとき、それが正しい切れ目かは中身を見ないと分からないので、
      画面で確かめる価値がある。1 ページずつには切れ目の判断が存在せず、ページ数がそのまま
      ファイル数になる。確かめるべきものが無い。**例外を作ったのではなく、元の判断の射程が
      N≥2 に限られていた**
- [ ] 「1 ページずつに分割…」を独立した操作にした（ツールバー「1 枚ずつ」）。押す → 保存先 →
      書き出しの 1 手。分割ダイアログに切り方を並べて 1 つにまとめる案は採っていない。
      全分割で 2 手増え、既存の区切り分割でも 1 手増える
- [ ] **区切りは見ないし、消しもしない。** `applyEveryNPages(1)` を内部で呼ぶ実装にはしていない。
      付けてある区切りを黙って壊すためである。「1 ページずつに」という文言が、区切りを
      見ないことを既に宣言している
- [ ] 1 ページの文書では操作そのものを無効にする。できるのは元と同じ 1 ファイルだけで分割に
      ならず、区切りが無いときと違って利用者に打つ手も無い。断るより初めから押させない
- [ ] 確認は挟まない。入力は変わらず、出力先に同名があれば 1 つも書かずに失敗する。
      枚数は状態表示が常に出しているので、何個できるかは押す前に読める
- [ ] **`PageCountPrompt` のボタンは「分割」と書いてあるのに分割していなかった**（区切りを
      入れるだけ）。「区切る」に直し、題も「N ページごとに区切る」にした。押した人が
      書き出されたと思い込む文言だった（`CLAUDE.md` 優先順位 2）
- [ ] `PageOperations.split` / `SplitStrategy` は今回も使っていない。UI が切るのは編集中の
      並びであり、上の判断は変わらない。M2 の CLI で使う

### 書き出しを元文書ベースに変えた（2026-08-21）

きっかけは「デジタル署名は不要か」という問いだった。調べたところ、署名より重い問題が
書き出し経路にあった。

- [ ] **`importPage` での詰め替えをやめた。** 新しい文書にページを詰め替えると、
      しおり・フォームの入力値・添付ファイル・文書情報・ページラベル・タグ構造が
      すべて落ちる。回転だけが元の文書を保存していて構造を保っていたが、
      デスクトップの保存は必ず `assemble` を通るため、**開いて何も変えずに保存しただけで
      全部消えていた**。同じ回転操作でも CLI なら残り、UI なら消えるという食い違いもあった
- [ ] **捨てたページが出力に残っていた。** `importPage` が作るのはページ辞書の浅い写しであり、
      注釈は元の文書のオブジェクトを参照したままになる。書き出しは参照から辿り着けるものを
      すべて書くため、目次リンクを持つ文書では捨てたページがファイルの中に残った。
      ページ一覧には出ないので、開いても枚数を数えても分からない。
      再現テスト（`NoLeakage`）を先に入れて事実を確かめてから直した
- [ ] **書き出しを 2 通りに分けた。** 単一入力で同じページを二度使わないなら元の文書を保存し、
      それ以外は切り詰めた複製を `PDFMergerUtility` で結合する。境界と保たれるものは
      `SPEC.md` §4.2.1 に書いた。UI で複製を作る操作はないため、1 ファイルの編集は
      必ず前者に乗る
- [ ] **宛先を失ったしおりは項目ごと落とし、子は繰り上げる**（`PageReferences`）。
      宛先だけ外して残すと、押しても何も起きないしおりになる。
      漏えいを止めるだけなら宛先を外すので足りるが、それは目次があるかのように見せかける
- [ ] **ページの並べ替えは `/Kids` を直接書き換える。** `PDPageTree` の追加・削除は
      ページから辿れる参照をすべて歩くため、**ページどうしが指し合う文書では戻ってこない**
      （`StackOverflowError`）。目次を持つ文書はどれもこの形であり、結合経路が丸ごと落ちていた。
      並べ替えに要るのは順序の入れ替えだけで、歩く必要がない
- [ ] **文書情報は先頭の入力のものを引き継ぎ、警告する。** 題名や作成者を 1 つに決める
      根拠がない。警告は出どころが 2 つ以上あるときだけ出す。同じ入力のページを
      複数回出すだけなら、文書情報は迷いようがない
- [ ] **電子署名は検知して警告するに留める。** 付与も検証も Non-goal のまま
      （`SPEC.md` §2.2）。署名はページの並びに紐づき、追記でしか保てないため、
      並べ替えでは原理的に保てない。保てないものを黙って壊さないのが目的である

### ファイル一覧からの解除（Phase 4）

- [ ] 一覧の各行に「×」を付けた。間違えて追加したときに「閉じる」で開き直すしかなかった
- [ ] **出どころ番号は詰める。** 番号は書き出しに渡す入力一覧の添字であり、
      外したぶん後ろを繰り下げないと別のファイルを指す
- [ ] 取り除くとページが 1 枚も残らない場合は失敗させ、並びは変えない。
      一覧は 2 ファイル以上のときだけ出るので、最後の 1 つを外す経路はそもそもない
- [ ] サムネイルのキャッシュは丸ごと捨てる。鍵が (出どころ, ページ番号) であり、
      番号が繰り下がると鍵の意味が変わる。付け替えるより捨てるほうが確実
- [ ] 描画依頼は依頼した時点の文書を捕まえる。描画のときに番号から引くと、
      その間に文書が外されていた場合に別の文書を描いてしまう

### 直した不具合（Phase 4）

- [ ] `PageOrder.pages()` が呼び出しごとに `unmodifiableObservableList` を作っていた。
      このラッパーは元の一覧を弱参照で監視するため、参照を保持しない限り GC で回収され、
      変更リスナが黙って呼ばれなくなる。**画面は開いた直後だけ正しく、しばらくすると
      更新が止まる**という追いにくい壊れ方をする。
      旧 UI では `ListView.setItems` がラッパーを保持していて表に出なかったが、
      同じ書き方をしていた状態表示（`N / M ページ`）の更新は既に壊れていた。
      ビューは 1 つだけ作って使い回す。毎回同じインスタンスを返すことをテストで固定した
- [ ] **見えているサムネイルをクリックしただけで一覧が飛んでいた**（v0.1.0 の実機確認で発覚）。
      選択が動くたびに `ListView#scrollTo` を呼んでいたのが原因。
      **このメソッドは名前に反して「見えるようにする」ではなく、その行を viewport の最上段へ送る。**
      実体は `ScrollToEvent.scrollToTopIndex()` を投げるだけで、受けた `VirtualContainerBase` が
      `VirtualFlow#scrollToTop(int)` を呼ぶ。見えている行に対して呼んでも容赦なく飛ぶ。
      同じクラスの `VirtualFlow#scrollTo(int)` のほうが目的に合い、行が見えていれば何もせず、
      はみ出していれば足りない分だけ動かす。`.virtual-flow` を引いてそちらを呼ぶことで、
      可視判定を自前で持たずに済んだ
- [ ] **上に伴い、スクロールは「選択が画面の外へ出る操作」だけの責務にした。**
      クリックとドラッグの開始は利用者がタイルの位置を見て決めた操作であり、そこで一覧を動かさない。
      矢印キー・ドロップ後・文書を開いた直後・ページが減って選択が末尾へ寄ったときだけ追いかける。
      `select` はスクロールせず、`selectAndReveal` が追いかける側という分け方にしてある
- [ ] **選択中のページがある状態で別のサムネイルを掴むと、掴んだのと違うページが動いていた**
      （同じく実機確認で発覚）。原因は、選択が動くたびに呼んでいた `ListView#refresh()` である。
      **あれは全セルを作り直す。** `Properties.RECREATE` を立てると `ListViewSkin` が
      `requestLayout` し、`VirtualFlow#layoutChildren` の `needsRecreateCells` の枝が
      `sheet.getChildren().clear()` と全セルの `updateIndex(-1)`、`cells.clear()` /
      `pile.clear()` を行う。`updateIndex(-1)` は `RowCell.updateItem(null, true)` を通って
      `ThumbnailTile.clear()` に届き、タイルの位置を `-1` に落とす。
      ドラッグは MOUSE_PRESSED → DRAG_DETECTED → DRAG_DROPPED と続く一連のジェスチャで、
      **その間にレイアウトパスが挟まる。** 掴んだ時点のタイルが別のページを受け持った後に
      `startDrag` がフィールドを読むため、掴んだのと違うページが dragboard に入っていた。
      `startDrag` 自身が冒頭で `grid.select` を呼んでおり、掴んだ瞬間に掴んだ相手を
      作り直す予約もしていた
- [ ] **選択の反映を `refresh()` からタイル自身に移した。** 選択が動いて更新が要るのは
      旧選択と新選択の 2 枚だけであり、全セルを作り直す理由がない。`ThumbnailTile` が
      `selectedIndexProperty` を見て自分で枠を当てる。`show` からは「選択中か」の引数を落とし、
      選択位置の出どころを一つにした。両方から当てられると、呼び忘れた経路で枠が残る。
      `rebuild()` の中の `refresh()`（出どころの帯が出る／消えるとき）は残してある。
      あれは内容の変わらないセルを更新するために要り、選択とは別のタイミングで走る
- [ ] **あわせて、ジェスチャの間はタイルの位置をローカルに確定させることにした。**
      `startDrag` / `completeDrag` / `select` の先頭で一度だけ読む。タイルが行セルに
      使い回される以上、ジェスチャをまたいでフィールドを読むのは危うい。上の修正で
      作り直しは起きなくなったが、**次に誰かが `refresh()` を足したときに同じ壊れ方をしない**
      ための保険であり、コストはない
- [ ] `ListView#scrollTo` と `ListView#refresh` で 2 つ続けて踏んでいる。どちらも名前から
      想像する動きより激しく、素の `ListView` の上に格子を作る以上は避けて通れない。
      同じ穴に落ちないよう、`selectedIndex` の宣言と `reveal` の Javadoc に理由を書いてある
- [ ] **ステータスバーに「AI 機能: 無効」と出していた**（同じく実機確認で発覚）。
      AI が無いことを隠さない意図（INV-3）だったが、**「無効」は「有効にできるが今は切ってある」と読める。**
      この版には AI 機能そのものが入っておらず、有効にする設定はどこにもないため、
      利用者は在りもしない設定画面を探すことになる。README とリリースノートの
      「この版に AI 機能は入っていない」という説明とも食い違っていた（優先順位 2）
- [ ] **出す先をバージョン情報のダイアログに移し、文言を「この版には含まれていません」にした。**
      ステータスバーは開いている文書の状態を出す場所であり、版の性格を混ぜると読み分けられない。
      加えて v0.1.0 では常に同じ文字列しか出ず、その行を占める意味がなかった。
      文言は `AppInfo#aiStatus` に置いてテストで固定してある（「無効」と書かないこと）。
      `AboutDialog` には `AiProvider` ではなく可否の `boolean` だけを渡す。
      この画面が要るのは可否だけで、提案の仕組みまで知る必要はない

### パッケージング（Phase 4）

- [ ] jlink のルートモジュールを定数として build ファイルに固定した。jdeps での導出コマンドは
      コメントに残してある。依存を足したときは導出をやり直すこと。
      `jdk.localedata` はサービス経由の読み込みで jdeps が検出できないため手で足してある。
      外すと `ja` ロケールが消えて日付・数値の書式が英語圏のものに化ける
- [ ] `--include-locales=en,ja` でロケールを絞った。UI が日本語であり、それ以外の環境では
      英語にフォールバックする。他言語の書式が必要になったらここを広げる
- [ ] インストーラに渡す `--description` だけ ASCII にした。MSI のサマリ情報は
      コードページ 1033 で書かれ、日本語を渡すと最初の非 ASCII 文字以降が黙って落ちて
      `PDF ` だけが残る。切れた文字列を配るより正直である。
      ランチャー exe のバージョン情報（タスクマネージャに出る）は日本語のまま
- [ ] `.pdf` のファイル関連付け（`--file-associations`）は行わない。既定の PDF ビューアを
      奪う挙動であり、利用者の意図しない変更になる。起動引数で開く経路は既にある
- [x] ~~POI は v0.1.0 では一切使われないが依存に残してある（約 14MB）。M1 の XLSX 出力で
      要るものであり、外して戻すほうが build を余計に触ることになる~~
      → **2026-08-22 に外した**（上の「使っていない POI を外した」）。
      「build を余計に触る」より、**使っていないものを配って CVE を見続ける**ほうが高くついた——
      配布物の 14MB と Dependabot の警告 2 件が、すべてここから出ていた
- [ ] `dist/` を出力先にした。`.gitignore` が `/dist/` と `*.msi` / `*.exe` を除外済み
- [x] ~~ランタイムイメージからコンソールで動かすと
      `ERROR Log4j API could not find a logging provider.` が stderr に出る。
      commons-logging（PDFBox）が classpath 上の log4j-api を見つけ、その実装が無いため~~
      → **2026-08-23 実測。POI を外したら消えた。**
      **log4j-api は POI 経由で入っていた**（poi → log4j-api 2.24.3）。
      いま同梱されるのは 7 本だけで、log4j 関連は 1 つも無い。
      ランタイムイメージから `--add-modules=javafx.controls,javafx.swing -cp app/*` で
      起動し、**stderr が空である**ことを確かめた。
      **★ M1 で POI を戻すと再発する。** ロギング方針はそのときに決める
      M1 まで、実装を足して黙らせることはしない

### 使っていない POI を外した（2026-08-22）

**M1（Excel 出力）で戻す。** `SPEC.md` §85 の「Excel 出力 = Apache POI (SXSSF)」は
変えていない。仕様としての POI は残っている。外したのは依存の宣言だけである。

外した理由は、**1 行も使っていないのに配られていた**こと。

| | POI 込み | POI 抜き |
|---|---:|---:|
| 配布 ZIP | 61 MB | **44 MB** |
| 同梱する jar | 20 本前後 | **7 本** |
| ランタイムイメージのモジュール | 19 | **17** |

- **実際に使っている PDFBox（約 4 MB）の 3.5 倍**を、使っていない POI（約 14 MB）が占めていた
- **Dependabot の警告 2 件の出所がすべて POI 経由だった**（`log4j-api` 2.24.3 /
  `commons-lang3` 3.16.0）。POI を 5.5.1 へ上げても `commons-lang3` しか直らず、
  `log4j-api` は 2.24.3 のまま残るため、constraint での強制が要る状態だった
- **jlink のルートモジュール 2 つも POI のためだった**——`jdk.unsupported`
  （POI が `sun.misc.Unsafe` を使う）と `jdk.xml.dom`（xmlbeans が要求する）

`CLAUDE.md`「依存を放置しない」（org 基準 §2.2）は、**宣言している以上
CVE を見る責任があるという意味**である。使っていないものを見続ける理由はない。

#### 戻すときにやること

1. `pdf-core/build.gradle.kts` に `implementation(libs.poi.ooxml)` を足す
   （`gradle/libs.versions.toml` の `poi-ooxml` は座標として残してある）
2. `pdf-desktop/build.gradle.kts` の `runtimeModules` に `jdk.unsupported` と
   `jdk.xml.dom` を足す。**これを忘れると jlink 後のアプリが実行時に落ちる**
3. **`ArchitectureTest` の入れ子クラス `Poi` から `@Disabled` を外す。**
   POI がクラスパスに無い間、あのルールは常に真になる（下の「ルールの空振り」）
4. `XSSFWorkbook` ではなく **SXSSF を使う**（`CLAUDE.md` リソース管理節）

#### ★ 外したときに分かったこと ── ルールの空振り

**`noClasses().should().dependOnClassesThat()` は、依存先が 1 つも無ければ常に真になる。**
POI を外した時点で `poiMustNotLeakOutOfCore` は**緑のまま何も守らなくなっていた**。

既存の番人（`importedClassesAreNotEmpty`）では捕まらない。あれが見ているのは
**pdfjig 自身の取り込みが空になる場合**で、ライブラリを 1 つ外しても pdfjig のクラスは
全部読み込まれるためである。**指摘は org 側から来た**（`propagandist/.github#43` →
PR #46 のコミットメッセージ。`security-verification.md` §1.3 の
「`-`（未走査）を 0 件と読むな」と同じ型だと書かれている）。

外部ライブラリを対象にするルールには、**そのライブラリへの依存が 1 つ以上あることを
確かめる対のテスト**（`...RuleHasSubject`）を隣に置いた。2026-08-23 に実際に
`@Disabled` を外して確かめてあり、**ルールは PASSED のまま、ガードだけが FAILED になる**。

```
Apache POI の型が pdf-core の外から見えない  PASSED   ← 空振り
POI のルールが空振りしていない              FAILED   ← ガードが検知
```

**★ 同じ形は PDFBox にもあり得る。** いまは使っているので空振りしていないが、
外せば同じことが起きる。だから PDFBox 側にも対のガードを置いてある。

---

### UI テストの自動化（2026-08-22）

**Phase 3 の「JavaFX を必要とする部分はテストしない」を覆した節である。**
覆した理由と、そのとき踏んだ罠を残す。踏み直さないために書いてある。

#### なぜ覆したか

`pdf-desktop` の画面まわりは約 3,000 行あり、動作の担保は 4-4 の手動チェックリストだけだった。
「Monocle が要るから無理」という前提を確かめ直したところ、**Monocle は要らなかった**。

#### 構成

2 つの層に分かれる。**それぞれが答える問いが違う。**

| | 何を答えるか | どこで |
|---|---|---|
| `:pdf-desktop:uiTest`（TestFX） | 画面の操作が正しい結果を出すか | `build` の windows ジョブ |
| `tools/smoke/Verify-AppImage.ps1` | 配った成果物がその環境で立ち上がるか | `release` |

- **ヘッドレスにしない。** `org.testfx:openjfx-monocle:21.0.2` は JavaFX 21 で
  `Window#_updateViewSize` を実装しておらず、表示時に `AbstractMethodError` になる
  （[TestFX/Monocle#97](https://github.com/TestFX/Monocle/issues/97)）。
  windows ランナーは対話セッションを持つのでそのまま動く。
  配布対象と同じ描画経路を通る点でも、こちらが正直である
- **`uiTest` は独立したソースセットにしてある。** `build` に含めないことを構造で担保する。
  ワークフローの `if` だけに頼ると、`build` に紛れ込んだとき黙って ubuntu でも動き出す
- **フィクスチャは `pdf-core` の testFixtures から取る**（`TestPdfs`）。
  リポジトリに PDF を置けない（INV-6）以上、生成の作法が 2 か所に分かれると必ず食い違う。
  そのため `pdf-core` に `java-test-fixtures` を入れ、`TestPdfs` をそちらへ移した

#### そのために本体へ入れた変更

**守るべき規則は `CLAUDE.md` に置いてある**（「命名」「JavaFX」「テスト」の各節）。
id の付け方、`setAccessibleText` の対象、不安定なテストをどこまで吸収してよいかは、
そちらが正である。ここに残すのは、なぜそれが要ったかだけである。

- **`FileDialogs` / `NativeFileDialogs`** — Windows の共通ダイアログは自動テストから
  操作できない。ここを差し替えられなければ「開く → 編集 → 保存」が通せない。
  UI テスト以前に、ネイティブ依存が 1 型に集まる設計改善でもある
- **`setId`** — それまで 1 度も使っていなかった。文言で節点を掴むと、画面の言い回しを
  変えるたびにテストが落ちる。`Action` レコードに id を 1 つ足し、メニューとツールバーの
  双方へ配っている。両者が同じ `Action` から作られる構造をそのまま使った
- **`setAccessibleText`** — ツールバーのボタンのみ。理由は次項

#### 踏んだ罠

**★ UI Automation から `setId` は見えない。**
JavaFX は UIA プロバイダを自前で持つ（`WinAccessible`）が、`UIA_AutomationIdPropertyId`
が返すのは `"JavaFX" + 内部連番` であり `Node#getId()` ではない（jfx21 のソース、
コメントも "Not required but useful for debugging"）。外から掴めるのは
Name（`AccessibleAttribute.TEXT`）と ControlType だけである。
**したがって起動スモークで深い操作を書くと、画面の文言を変えた瞬間に壊れる。**
「窓が出て、ツールバーが組み上がった」で止めてあるのはそのため。

**★ 窓を持つのは `Start-Process` が返すプロセスではない。**
jpackage の Windows ランチャーは自分の子として本体を起こす。実測では親は窓を持たず、
子が `PDFjig` という表題の窓を持っていた。返り値の PID だけを見ると必ず落ちる。
起動によって増えた同名プロセスを全部候補にしている。

**★ 起動スモークは Windows PowerShell 5.1 で走らせること。**
`UIAutomationClient` / `UIAutomationTypes` は .NET Framework のアセンブリであり
.NET Core には無い。`release.yml` の既定シェルは `pwsh` なので、
このステップだけ `shell: powershell` を明示している。外すと `Add-Type` で落ちる。
スクリプト自体も **UTF-8 BOM 付きで保存すること。** 5.1 は BOM 無しを ANSI として読み、
UIA へ渡す「開く」が化けてボタンが永遠に見つからなくなる。

**★ 活性でない窓への最初のクリックは消える。**
Windows は非活性の窓への 1 回目のクリックを窓の活性化に使い、その下のコントロールには
届けない。`@BeforeEach` で前面に出していないと、「クリックしたのに何も起きない」形で
ときどき落ちるテストになる。

前面に出すのは OS 側の仕事であり、`Platform.runLater` が返った時点では終わっていない。
**前面化だけでは足りず、10 回に 1 回ほど取りこぼす。** 「開く」は押した結果が起きたかを
見て、起きていなければ押し直している（`DesktopUiTest#clickUntilAccepted`）。

これは環境の側の揺れであり、吸収してよい類のものである。何を吸収してよく何を
吸収してはならないかは `CLAUDE.md`「不安定なテストの扱い」に書いた。
この事例はそこで挙げている「吸収してよい例」の実体にあたる。

**★ TestFX の `@Start` / `@Stop` は継承されない。**
`ApplicationExtension` は宣言されたメソッドの中からしか探さない。土台クラスに置いても
呼ばれず、フィールドが `null` のまま全部落ちる。`DesktopUiTest` は `setUp` / `tearDown`
を提供し、`@Start` / `@Stop` は各テストクラスが自分で持っている。

**★ 節点は「見つかる」だけでは押せない。**
ダイアログの節点は窓が出る前から存在している。そこへクリックを送ると
「1 nodes, but no nodes were visible」で落ちる。窓が出て、割り付けが済んで、
大きさが入るところまで待つ（`DesktopUiTest#waitForNode`）。

#### 計画と違ったこと ── そして結局、計画のほうが正しかった

**ドラッグ&ドロップは駆動できる。だが CI では使えない。**

着手前は「TestFX は JavaFX の DnD を駆動できない」という前提で、並べ替えは自動化の
対象外にするつもりだった。手元で試すと `drag().dropTo()` で `startDragAndDrop` からの
一連が最後まで通り、書き出した PDF のページ順まで確認できたので、その判断を覆した。

**2026-08-22 の CI 実走で、この 1 本だけが 19 分 20 秒かかった**（他の 24 本は合計 61 秒）。
`build` は 23 分 2 秒で、`timeout-minutes: 30` に対して余裕が 7 分しかない状態になった。
テストは通っている。**落ちるのではなく、遅い。**

原因は TestFX の造りにある。`MoveRobotImpl` はマウスの移動を距離ぶんのステップに刻み
（既定で最大 200）、各ステップを `asyncFx` で JavaFX スレッドへ積む。ドラッグの最中、
そのスレッドは Windows の DnD のモーダルループにいて、積まれた移動を 1 つずつしか捌けない。
ランナーでは 1 つあたり約 5.8 秒（1160 秒 ÷ 200 ステップ）。最後の `move()` がその全部を待つ。

**設定では回避できない**（どちらも実測）。

| 試したこと | 結果 |
|---|---|
| `testfx.robot.move_max_count=1` | **22 本が落ちる。** 刻まないとクリックも DnD も成立しない。JavaFX はマウスの移動を連続したイベントとして見る |
| `testfx.robot=awt`（FX スレッドを経由しない） | **22 本が落ちる。** 手元でも動かない |

距離を詰めればステップ数は減るが（`min(distance, 200)`）、20px まで詰めても 2 分かかる。
しかも 5.8 秒/ステップという値は環境依存で、悪化しても気づけない。

**そこで `ReorderUiTest` は CI から外した**（`@DisabledIfEnvironmentVariable(named = "CI")`）。
手元では 2 秒で通るので消してはいない。**腐ることは承知の上であり、
4-4「人が見るもの」に CI で守られていないことを明記してある。**

**★ この一連で、手元で通ったことを CI で通る根拠に使う誤りを 2 度踏んだ。**
1 度目は「windows ランナーは対話セッションを持つから headed で動く」（結果的に正しかったが、
確かめずに書いた）。2 度目がこの DnD である。**手元と CI は別の環境であり、
片方の結果はもう片方の根拠にならない。**

#### 自動化していないもの

4-4「人が見るもの」を参照。**そこに挙げていないものは自動で見ている、とは読まないこと。**
AI 機能の入口は今この版に無いため、INV-3 について確かめられるのは
「AI 無しで一連の操作が通る」ことと「無いことを画面が正直に伝える」ことの 2 つだけである
（`AiAbsenceUiTest`）。AI を使う画面が入った時点で、その入口が `isAvailable()` で
隠れることをそこに足すこと。

---

### uiTest を手元で隔離しようとして、できないと分かった（2026-08-23）

**`./gradlew :pdf-desktop:uiTest` を手元で叩くと、TestFX の Glass Robot が実マウスを動かすため
開発機を数分ぶん取り上げられる。** 画面をロックしていると落ちる。
これを消そうとして 3 つの構成を実測し、**1 つの Windows セッションの中では消せない**と分かった。
**同じ道を二度掘らないために、測った値ごと残す。**

#### 測った結果

同じテスト 29 本・同じマシン。カーソルはホスト側で `GetCursorPos` を 400ms ごとに標本化した
（いずれもユーザー入力なしの状態で測っている）。

| 構成 | ホストのカーソル | uiTest |
|---|---|---|
| ホストで直に実行（従来） | 奪われる | 通る |
| Windows Sandbox・窓が前面 | **奪われる**（22 か所へ移動） | **29/29 通る**（431.8 秒） |
| Windows Sandbox・最小化 | **守られる**（741 標本すべて同一座標） | **26/29 落ちる** |
| 別デスクトップ（`CreateDesktop`） | **守られる**（同上） | **26/29 落ちる** |

落ち方はどれも同じで、`#tool-open を 5 回押しても届かなかった` と
`the query "…" returned no nodes`——**クリックが 1 つも届いていない。**

#### なぜ両立しないのか

**TestFX は実ハードウェア相当の入力（`SendInput`）を使い、それはセッション単位で
「入力デスクトップ」にしか届かない。**

- Windows Sandbox を最小化すると、ゲストのセッションが入力デスクトップを持たなくなる
- 別デスクトップを作っても、それはそのセッションの入力デスクトップではない

そして入力が届く状態にすると、その代償としてカーソルが共有される。
Sandbox の場合は RDP クライアントがホストのカーソルを同期するためである。

★ **「窓が見えていなければ隔離されている」は成り立たない。**
GitHub の windows ランナーは画面が見えないのに uiTest が通るが、
**あれはランナーのデスクトップがそのセッションの入力デスクトップだから**であって、
非表示のデスクトップでも入力が届くという話ではない。着手前にそう推論して、外した。

#### 残っている道

**別セッション**——`vmconnect` を閉じた Hyper-V の VM ならゲストが自分の入力デスクトップを持ち、
ホストのカーソルに触る経路が無い。**やるかどうかは Issues に起票してある。**

#### Windows Sandbox には別の使い道が残った

隔離としては使えないが、**閉じれば消える Windows** としての価値は別にある。
`tools/sandbox/` はそのために残してある。

- `Invoke-UiTestInSandbox.ps1` —— **カーソルは奪われる。**それでも、ホストの `build/` と
  `~/.gradle` を汚さない・画面をロックしたままでも走る・`ReorderUiTest` が 3.4 秒で通る
  （CI の windows ランナーでは 19 分 20 秒かかり、外してある）
- `Invoke-InstallCheckInSandbox.ps1` —— インストーラを入れて消す。**こちらはマウスを使わない**
  （`msiexec` はサイレント、起動確認は UI Automation の照会だけ）ので、上の制約と無関係に働く。
  4-4「人が見るもの」の 2〜5 番にあたる。**2026-08-23 に実測で一巡通した（61 秒）**

| 見たこと | 実測 |
|---|---|
| UpgradeCode が `build.gradle.kts` の `upgradeUuid` と一致 | 一致 |
| MSI がサイレントで入る・**再起動を要求しない** | 終了コード 0（3010 でない） |
| MSI は `%ProgramFiles%\PDFjig` に入る | 入った |
| スタートメニューに出る | `%ProgramData%\…\Programs\PDFjig\PDFjig.lnk` |
| 入れたものが起動して画面が組み上がる | 通った |
| 同じ MSI をもう一度入れると上書きになる | アンインストール情報は 1 件のまま |
| 消して残らない（ファイル / ショートカット / レジストリ） | 残骸なし |
| EXE がサイレントで入る | **`/qn` が効く**（実測）。`%LOCALAPPDATA%\PDFjig` に入る |
| EXE も消して残らない | 残骸なし |

**★ 検知が空振りしていないことも確かめてある。** わざと違う UpgradeCode を渡すと落ちる
（`-ExpectedUpgradeCode` で差し替えられる）。**通ることだけを見ても、
検知できる保証にはならない。**

**★ 確かめられていないことがある。** Sandbox の既定ユーザー `WDAGUtilityAccount` は
Administrators のメンバーなので、**「標準ユーザーの環境で入るか」は分からない**。
4-4 の 2 番のうち、機械で見えているのは「非対話で入る」ところまでである。

**★ CI へ足すかは決めていない。** Windows Sandbox は GitHub ホストランナーでは使えない
（入れ子仮想化が無い）ので、載せるならランナーへ直に入れて直に消す形になる。Issues に起票してある。

#### 構成の要点

- 書ける先は `C:\out` ひとつだけ。ソースは読み取り専用でマップして `C:\work` へ写してから組む
- 依存は `GRADLE_RO_DEP_CACHE`（`~/.gradle/caches` を読み取り専用でマップ）から引き、
  **ネットワークは切ってある**。4-4 の 6 番「ネットワークを遮断したまま動く」を兼ねる
- **`gradlew` は使わない。** 中の `GRADLE_USER_HOME` は空なので Gradle 本体を毎回
  ダウンロードしにいく。マップした `wrapper\dists` の展開済み `gradle.bat` を直接呼び、
  どの dist かは `gradle-wrapper.properties` から解く
- 中側は `exit-code.txt` を**最後に**書く。これが「置き場へ全部出し終えた」印であり、
  ホストはこれを見て Sandbox を閉じにかかる。先に書くと写している最中に窓を落とされる
- 所要は cold 5 分 / warm 4 分（起動を含む）

#### 踏んだ罠

**★ Sandbox の中は Windows PowerShell 5.1 だけである。** pwsh は入っていない。
三項演算子も `??` も使えない。`.ps1` は **UTF-8 BOM 付き**で保存する——
無いと 5.1 は ANSI として読み、日本語が化ける。

**★ native コマンドの stderr を `2>&1` でパイプへ流すと、`ErrorActionPreference = 'Stop'` の下で
ErrorRecord が終端エラーになる。** `java -version` も Gradle の警告も stderr へ出すので必ず踏む。
`Invoke-Native` を通すこと。（`java` は `--version` なら stdout に出る。）

**★ `WindowsSandbox.exe` は起動要求を出すだけで、受け付けられなくても何も返さない。**
確かめずに待ちへ入ると、上限いっぱい黙ったまま「ハングした」という誤った診断だけが残る。
`Invoke-Sandbox` は窓が現れたことを見てから待ちに入る。

**★ Sandbox は同時に 1 つしか動かない。** 前のプロセスが消えても VM の後片付けは続いており、
詰めて次を起こすと黙って失敗する。

**★ PowerShell は `[string]` 引数の `$null` を空文字に変換する。**
`CreateProcess` の `lpApplicationName` を省略したつもりが `ERROR_INVALID_NAME` になる。
別デスクトップのスパイクで踏んだ。

#### Sandbox が炙り出したテストの弱さ

cold run（warm より 3 割遅い）で 2 本が落ちた。どちらも**速い環境ならたまたま間に合う**
書き方をしていたところで、直してある（`fix(uitest): ダイアログの答えを消費するクリックを押し直しに通す`）。

- `saveAs` が `#tool-save` を素のクリックで押していた。**取りこぼしたクリックは待っても届かない**ので、
  上限を延ばしても直らない類である
- 「足したページは並びの末尾に付く」が `waitForFxEvents` だけで assert していた。
  あれはイベントキューを捌くだけで、背景で走る読み込みの終わりは待たない

**遅い環境は、速い環境で隠れている条件不足を見せてくれる。**

---

### 整形を Spotless に寄せた（2026-08-22）

整形の判断を機械に移した。**手で整えない。コミット前に `./gradlew spotlessApply` を掛ける。**

#### 改行が混ざっていた

`.gitattributes` が無く、`core.autocrlf` は手元で `false`、global で `input`、system で `true` と
三層で食い違っていた。書いた環境の改行がそのまま index に入り、**LF 82 / CRLF 16 の混在**に
なっていた（`README.md`、`docs/*.md`、`pdf-desktop` の Java 8 本、`pdfjig.css` などが CRLF）。

`* text=auto eol=lf` を正とし、Windows のシェルが読む `*.bat` / `*.cmd` / `*.ps1` だけを
チェックアウト時に CRLF へ戻す。`gradlew` は ubuntu ランナーが `sh` で実行するため明示的に固定した。

★ **Spotless を素で入れてはならなかった。** `lineEndings` の既定は `.gitattributes` が無いと
git の設定へフォールバックし、Windows では CRLF 側に倒れる。LF の 82 ファイルを書き換えに行き、
ubuntu と windows で判定が食い違って CI が割れる。**取り決めを先に置き、Spotless には参照させる。**

★ Gradle では `LineEnding.GIT_ATTRIBUTES` を指定できない（例外になる。
[spotless#1274](https://github.com/diffplug/spotless/issues/1274)）。使えるのは
`GIT_ATTRIBUTES_FAST_ALLSAME` だけであり、これは **対象が全部同じ改行** という前提で判定を省く。
その前提は「CRLF で残す `*.bat` / `*.ps1` を Spotless の対象に入れていない」ことで成り立っている。
**それらを target に足すと、混在を最初の 1 ファイルで代表して黙って改行を壊す。**

#### 何を選んだか

| 対象 | 何を使うか | なぜ |
|---|---|---|
| Java | palantir-java-format（4 スペース / 120 桁） | 既存が 4 スペース。100 桁だと 530 行が折り返される |
| `*.gradle.kts` | ktlint（`ktlint_official`） | 既定のまま。スタイルの好みで例外を作らない |
| md / yml / toml / css | 行末の空白と末尾の改行だけ | 整形はしない。`.md` の意図的な行末 2 スペースは 0 件と確認済み |

- **palantir は Javadoc を整形しない。** 懸念していた日本語の桁揃え（`@param path     入力ファイル`）は
  無傷で残った。google-java-format はこれを潰すので、**乗り換えるならその点を見ること**
- **`formatAnnotations()` は入れていない。** `@SuppressWarnings` の位置を動かす副作用が
  `-Werror -Xlint:all` と噛み合ったときに読みにくい
- ktlint は代入の右辺を改行して字下げする（`val x =` の次の行から `listOf(`）。
  Gradle DSL では見慣れないが、機械に任せた以上は例外を作らないことを選んだ
- 逃げ道として `toggleOffOn()` を有効にしてある（`// spotless:off` 〜 `// spotless:on`）。多用しないこと

適用の実差分は 549 行追加 / 578 行削除、41 ファイル。Java 8,513 行に対してこの程度で済んだのは、
既存が google-java-format 系の作法（static import 先頭、ASCII 順、ワイルドカード禁止）で
書かれていたためである。行末の空白と末尾改行の欠落は導入前から 0 件だった。

#### CI とワークフロー

`spotlessCheck` は `check` に自動で載るため、**既存の `./gradlew build` がそのまま検査になる**。
ワークフローもジョブも増やしていない（`CLAUDE.md` の CI 節）。

#### 版を追わせる仕掛け

palantir と ktlint の座標を `libs.versions.toml` の `[libraries]` に宣言してあるが、
**これはどのモジュールの依存にも入らない。** Spotless は自分の classloader で解決するため、
書いた座標は解決に使われない。`[versions]` だけでは Dependabot が座標を知らず版が固まるので、
**追わせるためだけの宣言である。消すと更新が来なくなる。**

版上げは `formatting` グループに寄せてあり、3 つまとめて 1 本の PR で来る
（`.github/dependabot.yml`）。連動して整形結果を変えるものを別々に受けると、
`spotlessApply` を 3 回掛けることになるためである。

**2026-08-22 の update job で実際に確かめた。** グループ化した直後に Dependabot が設定を
読み直して走り、次のことが分かった。

- **Dependabot は `[plugins]` を見ている。** `com.diffplug.spotless` は素の id のまま
  Dependency Snapshot に現れ、`formatting` グループに入った。マーカー座標
  （`…:com.diffplug.spotless.gradle.plugin`）にはならない
- **`[libraries]` に宣言しただけの palantir / ktlint も追われている。**
  「Dependabot に版を追わせるためだけの宣言」は狙いどおりに効いている
- ★ **`javafx = "21.0.7"` は追われていない。** Dependency Snapshot に出るのは
  `org.openjfx.javafxplugin`（プラグイン側、`version.ref` は `openjfx-plugin`）であり、
  `[versions]` の `javafx` はどの `[libraries]` からも `version.ref` されていないため
  視界に入らない。**JDK 21 に留める方針の間は実害がないが、誰も追っていないことは事実である。**
  追わせるなら `org.openjfx.jmods:jmods` を `[libraries]` に宣言する

#### 整形ツールの版が上がったとき

`formatting` グループの PR は **CI が落ちることがある。壊れているのではない。**
版が上がって整形規則が変わり、既存コードが新しい規則を満たさなくなっただけである。

1. PR のブランチを取り込む
2. `./gradlew spotlessApply`
3. 差分が出たら `style:` のコミットを足す
4. 差分が広範なら `.git-blame-ignore-revs` にそのコミットを足す
5. `./gradlew build` が通ることを確かめて受ける

★ **major の版上げは差分を見てから決めること。** ktlint 2.0 は `ktlint_official` の
ルールが変わり、受けると全 `*.gradle.kts` が書き換わりうる。

★ **自動で `spotlessApply` を掛けて push するワークフローは作っていない。**
`contents: write` を持つジョブを増やすことになり、得られるのは 1 コマンド打つ手間の
削減でしかない。**割に合わないと判断した。**

#### IDE を寄せる（.editorconfig）

IDE が違う形に整えると、`spotlessApply` のたびに書き戻される往復が起きる。`.editorconfig` で
IDE 側に同じ形を教えてある。

★ **これは IDE 向けの無害な追加ではない。ktlint はこのファイルを読む**
（`editorConfigPath` の既定がトッププロジェクトの `.editorconfig`）。そのため Kotlin セクションには
`ktlint_code_style`（ktlint の既定と同じ値）だけを書き、`indent_size` / `max_line_length` は
**書いていない**。既定から外れる値を置くと `*.gradle.kts` が一斉に書き換わる。
**触ったら `./gradlew spotlessApply` を掛けて差分が出ないことを確かめること。**

Java 側（`indent_size = 4` / `max_line_length = 120`）は IDE に palantir と同じ形を教えるためだけの
もので、palantir 自身は `.editorconfig` を読まない。

#### pre-push フックは入れていない

Spotless には `spotlessInstallGitPrePushHook` があり、push 時に `spotlessCheck` を走らせて、
崩れていれば `spotlessApply` を自動実行して push を中断する。**採らなかった。**

- 検査は既に 2 重にある——`CLAUDE.md` の規約（コミット前に `spotlessApply`）と、CI の
  `spotlessCheck`（ubuntu で 1 分 10 秒で結果が出る）
- ★ `.git/hooks` は git で共有されない。clone ごとに手で入れる必要があり、**入れ忘れると黙って
  効かない**。`uiTest` を「ワークフローの `if` ではなくソースセットで担保する」と決めたのと
  同じ理由で、**共有されない仕掛けより構造で担保された CI を採る**

欲しくなったら各自で `./gradlew spotlessInstallGitPrePushHook` を実行すればよい。
外すときは `.git/hooks/pre-push` を消す。

#### blame

改行の正規化と一括整形は `.git-blame-ignore-revs` に載せた。GitHub は自動で参照する。
手元では `git config blame.ignoreRevsFile .git-blame-ignore-revs` が要る。
**載せてよいのは中身を変えていないコミットだけである。**
整形と実装が混ざったコミットを載せると、blame から本物の変更まで消える。

---

### ブランチをどう使っているか（2026-08-22）

どこにも書いていなかったので残す。

- **`develop` が既定である。** GitHub の default branch も `develop`。開発はここに集まる
- 作業は `feature/` `fix/` `chore/` `ci/` を切り、`--no-ff` で `develop` へマージする。
  マージ済みのブランチは残さない（`git branch --merged develop` が `develop` だけになる状態を保つ）
- **リリースは `v*` タグで走る**（`.github/workflows/release.yml` の `on.push.tags`）。
  **ブランチは通らない。** 公開されるのは GitHub Releases に出るインストーラであって、
  ブランチではない
- ★ **`main` は「公開済みの版」を指す。** 2026-08-23 に v0.1.0 のタグから作った
  （それまでは作らなかった——まだ公開していないものを指す「公開用ブランチ」は嘘になる）。
  **次のリリースでは、そのタグのコミットまで `main` を進める。**
  保護は ruleset id 21228747（`deletion` / `non_fast_forward`）。理由は下の記録にある

### 撮影用サンプルの生成器（2026-08-24）

アイキャッチや説明用の画を撮るための PDF を `tools/sample/GenerateSamplePdf.java` で作る。

```bash
java tools/sample/GenerateSamplePdf.java [出力先]
```

出力先を省くと `tmp/sample-scan-bundle-12p.pdf` に書く。DELIVERY NOTE 3 枚 /
INSPECTION LOG 5 枚 / WORK ORDER 4 枚の計 12 ページで、**枚数はわざと不揃いにしてある**——
区切り → 分割のデモがそのまま撮れる。中身はダミー文字列だけで機密は含まない。

- ★ **生成物は追跡しない**（INV-6）。このツールは乱数も日付も使わないので、何度回しても
  同じバイト列が出る。**PDF を置かなくても同じ画を撮り直せる**から、`.gitignore` の `*.pdf` に
  穴を開けずに済んでいる。**乱数や日付を入れた時点でこの理屈が崩れる**ので、入れないこと
- **元は JS で書かれていた。** リポジトリに Node の資産が 1 つも無く、Spotless の管轄外に
  1 ファイル残ることになるので、`tools/icon/GenerateIcon.java` と同じ単一ファイル Java へ移した。
  移植で**出力は 1 バイトも変えていない**（JS 版の出力と `cmp` が完全一致。**2026-08-24 実測**）
- **PDFBox を使わずバイト列を手で組んでいる。** `tools/` は Gradle のビルドグラフの外にあり、
  単一ファイル起動では外部 jar を引けない。`GenerateIcon.java` も JDK 標準だけで書かれている
- ★ 手組みなので、**PDF として妥当かは PDFBox の目で見る**——`pdf-cli info` が `pages: 12` /
  `encrypted: false` を返すこと（**2026-08-24 実測**）。**xref が壊れていても開けてしまう
  ビューアがある**ので、画面で開けたことは根拠にならない

---

## 未決事項

**未決のものは GitHub Issues で管理する**（2026-08-23 に移した）。実装中に判断が必要に
なったら、**ここではなく issue を立てて確認を求めること**（`CLAUDE.md`「作業の進め方」）。
**両方に書くと必ず片方が腐る。**

**いつ決まるかはマイルストーンが持つ**（`CLAUDE.md`「作業の型」）。
**一覧をここに写さない。**

- 未決の一覧 — https://github.com/propagandist/pdfjig/labels/decision
- 期ごとの残り — https://github.com/propagandist/pdfjig/milestones

### 決まったことの記録

未決から落ちた項目を、判断の経緯ごと残してある。**消さないこと**——
「なぜそう決めたか」と「実測でこうだった」がここにしかない。

- [x] パッケージ名は `io.github.propagandist.pdfjig` に統一した（Phase 1 で確定）。
      Maven Central 公開時の groupId と揃えてあり、後の改名は不要
- [x] アプリケーション名の表示は `PDFjig` に決めた（Phase 4）。アイコンは治具が書類を
      挟んでいる図の仮アイコンを `tools/icon/GenerateIcon.java` で生成して同梱してある。
      意匠を差し替えたくなったら同じツールを回すか `.ico` を上書きする
- [x] Phase 4 の jpackage に必要な情報はすべて決まった（Phase 4）。表示名 `PDFjig`、
      ベンダー `PROPAGANDIST CORPORATION`、UpgradeCode は上の「確定している判断」を参照
- [x] 対応する最小 Windows バージョンは 10 21H2 (x64) とした（Phase 4）
- [x] ~~UI テストは CI でまだ実走していない~~ → **2026-08-22 に実走し、25 本すべて通った。**
      ただし `ReorderUiTest` が 19 分 20 秒かかり、`build` 全体が 23 分 2 秒になったため、
      同日そのテストを CI から外した（上の「UI テストの自動化」）
- [x] ~~起動スモークは release でまだ実走していない~~ → **2026-08-22 に `workflow_dispatch` で
      `version=0.0.2` を走らせ、通った**（3 分 26 秒）。ランナー上で ZIP から
      `PDFjig.exe` が起動し、UI Automation でツールバーの「開く」まで届いている。
      確認後、ドラフトリリースは消してある（4-3 のやり方）
- [x] ~~`uiTest` の所要時間の実測~~ → **2026-08-22 実測。`ReorderUiTest` を外して
      `uiTest` タスクが 15 秒、`build`（windows）全体が 5 分 0 秒**（外す前は 20 分 25 秒 /
      23 分 2 秒）。あわせて `timeout-minutes` を実測に合わせた（build 30 → 15 分、
      release 45 → 20 分）。**上限を実測より桁大きく取らないこと。**
      30 分の上限は、`uiTest` が 19 分かかっていた事実を隠していた
- [x] セキュリティ分類。**org 側に分類 P（利用者の手元で動くものを配る）が足された**ので、
      `CLAUDE.md` のセキュリティ節をそれへ揃えた（`propagandist/.github#22`、2026-08-22）。
      **「分類 D ＋ §5.3 も読む」という自前の運用は要らなくなった。**
      分類表には軸が 2 本あり、A〜D は「実行コードがどこで動くか」、L と P は
      「**管理外へ何を配るか**」——L は API を配り、P は実行物を配る。
      **③ 週次 cron を足すときの 3 条件も同時に決まった**（同 `#21` / `#38`）。
- [x] Spotless の `palantir-java-format` / `ktlint-cli` は**依存グラフに載せない**と決めた
      （#19、2026-08-23）。**できないからではない**——専用の解決可能 configuration を置けば
      載る（`Usage` / `Category` / `LibraryElements` / `Bundling` / `TargetJvmEnvironment` の
      5 属性を明示すれば解決することを実測で確認した）。ただし**推移依存 58 件が増えて
      SBOM が 64 → 約 120 とほぼ倍**になる。整形ツールはコードを生成せず、書き換えた結果は
      必ずコミット差分に出て人が見る。**配布物にも入らない**ので、見えても対処が変わらない。
      **alert が増えるほど、本当に見るべき同梱物（JDK / JavaFX / PDFBox 系）が埋もれる。**
      版は `libs.versions.toml` の宣言で追えている（上の「版を追わせる仕掛け」）
- [x] **configuration cache は有効にしない**と決めた（#20、2026-08-23）。効き目は手元で
      `build` **4 秒 → 約 2 秒**。CI は毎回コールドなので効くかどうかも確かめていない。
      対して直す対象は **6 タスク / 2 ファイル**で、うち 5 つは `jlink` / `jpackage` の
      **リリース経路そのもの**である。**2 秒のためにそこを書き換えない。**
      `:pdf-core:processResources` の 1 件は `build` 単体でも出るが、**1 件だけ直しても
      有効化はできない**（`packageAll` が残り 5 件で落ちる）ので触っていない。
      見直す契機は「構成フェーズが伸びたとき」か「CI でキャッシュが効くと確かめられたとき」
- [x] **マイルストーン運用を入れた**（#26、2026-08-23）。単位は**リリース版**（`v0.1.0` /
      `v0.2.0` …）で、`SPEC.md` §8 の M0〜M3 は**仕様側の期**として残る——**1 つの期が
      複数のリリースに分かれる。** M0〜M3 をそのまま名前にする案は、M1 が 13 issue を
      抱えた 1 本になって長期間閉じず、進捗を見せないので却下した。
      ★ **closed の issue にもマイルストーンを付ける。** 閉じたものを外すと
      「その期に何が起きたか」の記録でなくなる。値は `CLAUDE.md`「作業の型」にある
- [x] ★ **未決事項の表は、Issues へ移した翌日には既に腐っていた**（同上）。
      2026-08-23 に移したのに、**同じ日に閉じた #19 #20 が表に残っていた。**
      「両方に書くと必ず片方が腐る」の実例が、**それを書いたこの文書自身で起きている。**
      **消すのではなく、起きたこととして残す**——次に一覧を書き足したくなったときに読む
- [x] **v0.1.0 を公開した**（#27、2026-08-23）。タグ 72df4ad、`release` は **3 分 2 秒**で
      成功し、MSI 45.2 / EXE 45.8 / ZIP 44.1 MB の 4 点（`SHA256SUMS.txt` を含む）が付いた。
      ハッシュは手元で再計算して一致を確認し、**配る ZIP そのもの**を
      `tools/smoke/Verify-AppImage.ps1` で起動させてから公開した。
      これで `README.md` の「最新版は v0.1.0」が真になった
- [x] **捨てタグは `v0.1.0-rc1` の形では打てない**（同上）。`release.yml` の「バージョンを
      決める」が `^\d+\.\d+\.\d+$` で弾く。**通したいワークフローの入口で落ちるので、
      捨てタグを打つ意味そのものが無くなる。** `v0.0.3` で通した（**2 分 56 秒**）。
      `workflow_dispatch` も代わりにならない——**タグ名からバージョンを切り出す 1 行だけは
      dispatch では通らない。** 4-4 に書き足した
- [x] **捨てタグで 2 つ見つかった**（同上）。**これが捨てタグを打つ理由である。**
      ひとつは **`RELEASE_NOTES.md` の機能表に「1 ページずつに分割」が無かった**こと
      （2026-08-22 に独立させた操作。README も同じ）。もうひとつは
      **リリース本文の相対リンクが切れる**こと——`../SECURITY.md` はリリースページ
      （`/releases/tag/v0.1.0`）からの相対として解決され、`/releases/SECURITY.md` を指す。
      GitHub の Markdown API（`mode=gfm` + `context`）が href を相対のまま返すことで
      確かめた。`/security/policy` への絶対 URL にした——**版にもパスにも依存しない**
- [x] **`main` を作り、`main` だけを保護した**（#22、2026-08-23）。ruleset id 21228747。
      `deletion` と `non_fast_forward` の 2 つだけで、bypass は RepositoryRole=admin。
      ★ **`required_status_checks` は入れていない。** `**/*.md` の変更では `build` が
      走らないため、check run が存在しないコミットが `main` に来た瞬間に詰まる。
      **v0.1.0 が指す 72df4ad がまさに docs だけのコミットであり、入れていたら初回から
      通せなかった。** ★ **これで Check runs failure threshold が効き始めたわけではない**
      ——対象になる check run は code scanning が作るもので、それが無い状況は #23 のまま。
      **決めたのは「守る対象を固めた」ことだけである**
- [x] **#23（Code scanning）は v0.2.0 へ移した**（2026-08-23）。期限は「M1 の着手前」であり、
      **M1 は v0.2.0 / v0.3.0 / v0.4.0 に分かれている**ので、当たるのは v0.2.0 である。
      v0.1.0 に置いたままだと、公開が済んでも閉じられない issue が残り、マイルストーンが
      記録にならない。**判断を先送りしたのではなく、期限の側を正しく読み直した**
      → **2026-08-27 訂正: `v0.1.2` へ移した**（#83。下の「★ 訂正」に経緯がある）。
      **当時の読みは変えていない**——変わったのは「次の版」がどれかのほうである
- [x] **`distributed-artifact-watch` は一度も比較を実行していなかった**（#41、2026-08-23）。
      リリース資産が 0 件だった間は「配布済みが無いので抜ける」経路しか通っておらず、
      v0.1.0 の公開直後に回して初めて本体が動き、**その 1 行目で落ちた。**
      `PDFjig/runtime/release` は jlink が Windows で作るので **CRLF** であり、ubuntu で
      読むと `s/^JAVA_VERSION="\(.*\)"$/\1/p` の `"$` がマッチしない。
      **JAVAFX_VERSION 側はマッチしてしまう分だけ質が悪く、値に `\r` が混ざって版の比較が
      静かに狂う。** `tr -d '\r'` を読んだ直後に一度だけ掛けて直し、再実行で 38 秒・緑。
      ★ **「構造が変わったら緑ではなく赤にする」という同ファイルの判断が効いて、
      読めなかったことが赤で出た。設計は正しく働いている**
- [x] **`ReorderUiTest` は手元では 1 分 28 秒**（2026-08-23 実測）。**windows ランナーの
      19 分 20 秒とは桁が違う。** CI から外した判断（上の「UI テストの自動化」）の裏付けに
      なる数字だが、**逆に言えば手元で通ることは CI で通る根拠にならない**（`CLAUDE.md`）。
      4-4 の 9 番目は、テストが通ることとは別に**掴んだ感じを人が見る**必要がある
- [x] **既定ブランチは `develop` のままにする**と決めた（2026-08-23、v0.1.0 の公開直後）。
      `main` を作った直後なので「慣習に合わせて default を `main` へ」を検討したが、
      **default branch を見ている仕組みが 3 つあり、いずれも壊れる。**
      **`main` を default にしないことは、慣習より実測を採った判断である。**
  - **① 週次ウォッチが「守っているつもり」になる。** `distributed-artifact-watch` の
    `actions/checkout` は `ref` を指定していないので、**schedule のときは default branch を
    取る。** default が `main` だと比べる相手が「いま **`main`** から作れるもの」になり、
    **`main` はリリース時点を指すので配布物と常に一致する——差が永久に出ない。しかも
    赤ではなく緑で。** このワークフローが避けようとした状態そのものである。
    加えて **cron は default branch のワークフロー定義で走る。** 実測で
    **`main` には #41 の `tr -d '\r'` が入っていない**ので、**直したはずのバグで走り続ける**
  - **② 依存グラフと Dependabot alerts は default branch を基準にする。**
    2026-08-23 実測で `develop` の SBOM は **64 件**（上の Spotless の判断にある数字と一致）。
    `dependency-graph.yml` は `push: branches: ["develop"]` で送っており、
    default を動かすと**送り先と評価の基準が別のブランチになる。**
    分類 P で §3.12（依存の CVE）を担保しているのはこの経路だけである
  - **③ README と `SECURITY.md` は default branch から読まれる**（後者は GitHub の
    `/security/policy`。`docs/RELEASE_NOTES.md` のリンク先でもある）。
    **`main` はリリース間に動かない**ので、文書を直しても次のリリースまでトップに出ない
  - `build.yml` は `["main", "develop"]` の両方で走るので、**CI だけは影響を受けない。**
    「CI が緑だから安全」と読める形になっているのが、この判断の分かりにくいところである
  - **見直す契機**——リリース頻度が上がって `main` が `develop` に十分追随するようになるか、
    `develop` 常設をやめてトランクベースへ移るとき。**そのときは checkout の `ref` 明示・
    `dependency-graph.yml` のトリガー・cron がどの定義を読むかを同時に直すこと。**
    片方だけ変えると、①が**緑のまま**壊れる
- [x] 分割の部分出力にロールバックがない → **2026-08-21 に直した**（#48）。書く前に出力先を
      控えておき、途中で落ちたら控えたものを消してから元の例外を投げ直す。**書き終えたものだけを
      控えると書きかけのファイルが残る**ので、控えるのは書く前である。出力先が既に無いことは
      事前に確かめてあるので、消してよいのはこの呼び出しが作ったものだけだと言える。
      後始末そのものの失敗は握り潰す——元の失敗のほうが利用者にとって重要である
- [x] 保存しても「未保存の変更があります」が消えない → **2026-08-21 に直した**（#47）。
      `PageOrder.markSaved` に **書き出した並び** を渡す。**いまの並びを渡してはならない**——
      書き出しは非同期であり、その間に並べ替えられていればその分はまだ書き出されていない。
      書き出し中に別の文書を開かれていたら基準を動かさない。副作用として `reset()` の意味が
      「開いた直後に戻す」から「最後に書き出した状態に戻す」に変わる
- [x] `merge` が文書情報を引き継がない → **2026-08-21 に直した**（#46）。`PDFMergerUtility` は
      文書情報を入力ごとに埋めていき、**「題名は 1 つ目、作成者は 2 つ目」という、どこから
      来たのか説明できない文書**ができることが分かったため、`merge` と `assemble` の両方で
      先頭の入力のものに揃える。出どころが 2 つ以上あるときだけ警告する
- [x] 「保存」が既存ファイルを黙って上書きする件（#51）→ **2026-08-24 に文書の側を直した。**
      `RELEASE_NOTES.md` は「出力先に同名のファイルがあると失敗する」と約束していたが、
      `MainWindow` の保存は `REPLACE_EXISTING` で置き換えていた。**壊れていたのは文書の側である**——
      置き換え方式そのものは正しい（先に消すと書き込み失敗で原本を失う）。実装を文書に合わせる案は
      採らなかった。**ネイティブダイアログの「置き換えますか？」に「はい」と答えた直後に
      アプリがエラーを出す**ことになり、避けるには保存ダイアログを自前で作るしかない。
      挙動は 1 行も変えていないので再リリースは要らない。上書き方針の正本は `SPEC.md` §4.2 に置いた
- [x] `Files.exists` と書き出しの間の競合（TOCTOU）→ **2026-08-24、見ないと決めた。**
      `StandardOpenOption.CREATE_NEW` で原子的に弾く形は採らない。**利用者の手元で動く
      デスクトップアプリであり、出力先を選ぶのも同じ利用者である。** PDFBox の `save` は `Path` を
      受けるため、閉じるには `pdf-core` の全書き出し経路を `OutputStream` 経由に直すことになり、
      得るものに見合わない。**この前提が変わるのは `pdf-core` を publish したとき**（`SPEC.md` §9）
- [x] `OUTPUT_ALREADY_EXISTS` のメッセージがパスを含まず、分割でどれが衝突したのか特定できない
      → **2026-08-24、次の版へ送った。** 衝突したパスは機密ではなく利用者自身が選んだものなので
      INV-5 には触れないが、**v0.1.1 は「新機能は入れない」版である**（マイルストーンの description）。
      約束違反の修正ではなく使い勝手の改善であり、混ぜない
- [x] 「取り除いたはずの内容が出力に残る」が `SECURITY.md` の対象範囲に無かった（#59）
      → **2026-08-24 に足した。** 挙がっていた 4 つ（パスワード漏出／暗号化入力からの平文出力／
      細工 PDF による RCE／AI への送信）は**どれも #46 に当たらない**——入力は平文でよく、
      細工も要らない。**機密文書に及ぶ経路がパスワードと暗号化だけだと読んでいた**のが原因である。
      **ページの除去を名指しせず**「取り除いた・消したはずの内容が出力に残る」の形で書いた。
      M1 で暗号化・メタデータ・表抽出が入れば同じ形の経路は増え、名指しではそのたびに足すことになる
- [x] Advisory の深刻度をどう付けるか（#59）→ **2026-08-24、`CVSS:3.1/AV:L/AC:L/PR:N/UI:R/S:U/C:H/I:N/A:N`
      = 5.5 と決めた。** 帯は GHSA の画面では `Moderate`、REST API では `medium` と出る
      （**同じ帯の別表記である。API に `moderate` を渡すと 422 で弾かれる**）。
      **攻撃者が居ない**（利用者が自分で書き出し、自分で配る）ため素直には当てはまらないが、
      要素ごとの根拠は立つ——`AV:L` 遠隔の攻撃面が無く手元のアプリで完結／
      `UI:R` 書き出しと配布という利用者の操作が要る／`C:H` 外した 1 ページどころか元の文書が
      全ページ残っていた（#46 の実測）／`I:N` `A:N` 出力が壊れるわけではない。
      **`S:C`（影響が受け取った第三者へ及ぶと読む）を採ると 6.3 になるが帯は Moderate のままである。
      解釈の割れる要素で帯が動かないので `S:U` を採った。** ★ **次に似た件が来たらこの付け方に揃える。
      「なんとなく Moderate」にしないための記録である**
- [x] Advisory で CVE を要求するか（#59）→ **2026-08-24、要求しないと決めた。**
      **pdfjig はライブラリではなく、配布物は MSI / EXE である**（`SPEC.md` §9）。
      依存スキャナが追う座標を持たないので、**CVE を取っても利用者に届く経路は
      Releases の Watch と Advisory のまま変わらない。** 申請の往復が
      「v0.1.1 の公開と同時に出す」という段取りを縛るほうの害が大きい。
      ★ **`pdf-core` をライブラリとして publish した日にこの前提は崩れる**（`SPEC.md` §9）。
      そのときは CVE の要否から見直すこと
- [x] #46 の Advisory の下書き → **2026-08-24 に作った。`GHSA-xw3x-275f-ffcv`（`draft`）。**
      **公開は v0.1.1 の公開と同時**（`SECURITY.md`「修正版の届き方」の約束。#61 の順序表 順 7）。
      ★ **本文に「詳細は既に公開されている」ことを書いてある**——#46 は public な issue で
      再現条件まで出ており、**伏せていたものを公表するのではなく、既に出ているものを
      影響と対処の形に整理して届ける**という位置づけである。
      なお `GHSA-gwpq-rf4x-r38h` は 2026-08-23 の通知経路テスト（closed）であり、別物
- [x] **v0.1.0 は AI 機能がゼロのまま出した。** 2026-08-23 に公開している。
      **AI なしで実用に耐えるツールとして一度リリースした事実を履歴に残すこと**が、
      `CLAUDE.md` INV-3（AI 不在で全機能が動く）の裏付けになる。
      ★ もとは 4-3 に「この時点で AI 機能はゼロ。それで問題ない」と書いてあったが、
      **あれは手順ではなく v0.1.0 の記録だった**ので、2026-08-27 にここへ移した（#63）
- [x] **`SECURITY.md`「サポート対象バージョン」の未リリース記述は、v0.1.0 の公開時に消した。**
      もとは 4-3 の手順に固有の作業として書いてあったが、**済んでしまえば手順ではない。**
      2026-08-27 に「状態を書いた記述が腐っていないか見る」という一般形へ書き換えた（#63）。
      **消さずに残しているのは、なぜタグを打つ前なのかがここにしか無いためである**——
      タグと、そこから作る `main` が指すコミットに「まだ出ていない」を残さない
- [x] **内部の作業用テンプレート（org `work-conventions.md` §2 の 7 項目）は作らない**
      → 2026-08-27 に決めた（#60）。**既存 39 本すべてが `gh issue create --body-file` で
      書かれており、テンプレートを 1 本も通らない。** 加えて **7 項目の正本は org §2 である**——
      テンプレートへ写せばその日から正本が 2 つになり、org 側を直しても `.github/` の写しは
      直らない（org §0）。外の人に 7 項目は書けない。「判断とその理由」も「対象範囲」も
      **コードを持っている側の仕事**である。置いたのは**外から来るもの 1 枚だけ**にした
- [x] **Private Vulnerability Reporting の報告フォームには、リポジトリ側から手を入れられない**
      → 2026-08-24 確認（#60）。**調べ直すのに時間がかかる種類の否定なので残す。**
      設定できるのは**有効/無効と通知の受け取り方の 2 つだけ**で、フォームは GitHub 固定の
      下書きアドバイザリのものである（必須は title と description だけ）。
      GitHub が「テンプレート」として指す `github/securitylab` の `report-template.md` は
      **報告者が自分で description に貼るもの**であり、こちらが仕込めるものではない。
      ★ **したがって報告者が何を書くかに効かせられるのは `SECURITY.md` と
      issue 作成画面の contact link の 2 つだけ**であり、後者がこちらの文を置ける唯一の場所である
- [x] ★ **版を前に挿しても、後ろの版の中身は移らない**（#83、2026-08-27）。#26 が
      2026-08-23 にマイルストーンを 4 本作ったとき、`v0.2.0` は「`v0.1.0` の次の版」だった。
      **08-24 に `v0.1.1`（02:54）と `v0.1.2`（03:31）が前に挿し込まれたが、`v0.2.0` の中身は
      一度も見直されていない。**「次の版」の座が 2 つ前へ移り、中身は元の場所に残った。
      そこへ新しい issue も「いちばんそれらしい open な版」として入り続け、**08-27 実測で
      open 17 本のうち閉じる条件（暗号化）に関与するのは 5 本だけ**になっていた。
      **5 本を閉じれば条件は成立するのに 12 本が残る**——`v0.1.1` は 0 open で閉じた実績が
      あり、同じようには閉じられない。12 本を `v0.1.2` へ移し、両方の description を
      中身に合わせて書き直した
- [x] **`CLAUDE.md` は「どの版に付けるか」を答えていなかった**（同上）。答えていたのは
      「付けるか / 付けないか」だけである。**#79（08-27 起票、`v0.2.0`）は本文で
      #75（08-25 起票、マイルストーン無しのまま）を名指しし、「向こうと事情が違う」と
      書いた上で逆を選んでいる**——基準が無いので、どちらが正しいとも言えない状態だった。
      ★ **同じ日ではない。2 日空いている。** それでも #79 が先例を読んで逆へ振れたことは
      変わらない。しかも #79 は「着手が見えている版に載せる」と書いて `v0.2.0` を選んで
      いた——**その版は `v0.1.2` である。自分の論理に照らして 1 つずれていた。**
      軸を「**その版の description が持つ閉じる条件に関与するか**」に決めて 3 行足した
      → **2026-08-27 訂正: 軸を「その版が何を配る版か」に書き直した**（同日、下の
      「★ 閉じる条件が『配ったこと』を含んでいなかった」）。**閉じる条件に公開を入れた結果、
      どの版の条件にも全件が「関与」してしまい、軸として働かなくなったため**
      （`CLAUDE.md`「マイルストーン」）。**#79 の本文は消さずにコメントで訂正した**
      （org `security-verification.md` §5）
- [x] **閉じた issue のマイルストーンは、付け替えてよい**（同上）。#42 / #70 は 08-24 に
      閉じ、**コミットが `v0.1.1` のタグに含まれている**（`84e8161` / `16028b4`）のに
      `v0.2.0` に付いていた。**まだ始まっていない版が 2 本の実績を持っている状態である。**
      `CLAUDE.md` が禁じているのは「**外す**」ことであり、誤った版から実際に閉じた版への
      付け替えは記録を**正す**操作である。禁止の理由（記録であること・進捗の分母）が
      どちらも付け替えを支持する。★ **`gh issue edit --milestone` は closed のマイルストーンを
      引けない**（2026-08-27 実測。両方とも失敗した）。`gh api repos/.../issues/<N> -X PATCH
      -F milestone=<番号>` へ落とす。**タイトルではなく番号で指す**
- [x] ★ **訂正**——上の「**#23（Code scanning）は v0.2.0 へ移した**」（2026-08-23）は、
      **2026-08-27 に `v0.1.2` へ移した**（#83）。**あのときの読み（期限は「M1 の着手前」で、
      M1 は `v0.2.0` / `v0.3.0` / `v0.4.0` に分かれる）は変えていない**——`v0.1.2` は M0 の
      後始末であり、そこで片づけば M1 の着手前という条件は満たされる。**変わったのは
      「次の版」がどれかのほうである**
- [x] **`v0.1.x` に新機能を入れないのは `v0.1.1` だけの線である**（#83、2026-08-27）。
      `v0.1.2` へ移した中に #12（設定の永続化）／ #13（ログ）／ #72（更新経路）という
      機能追加が含まれる。`v0.1.1` の description が「新機能は入れない」と書いたのは、
      **あの版が書き出し経路を全面的に書き換えるものであり、別モジュールの回帰を混ぜない
      ため**であって、`v0.1.x` 全体の規律ではない。**`v0.1.2` の description に明示して
      引き受けた**——版数の読みと食い違うことを、文面の側で先に断っておく
- [x] ★ **閉じる条件が「配ったこと」を含んでいなかった**（#83、2026-08-27）。#84 を出した
      直後に、足した規約を open 35 本へ当てて分かった。**`v0.2.0` / `v0.3.0` / `v0.4.0` の
      3 本だけ、閉じる条件が中身しか縛っていない。**
      ★ **「リリースがまだ 1 本も無かったから」ではない。実測がそう言わない**——
      `v0.1.0` と `v0.2.0` は**同じ秒に作られている**（どちらも 2026-08-23T04:38:11Z。
      `v0.3.0` が :13、`v0.4.0` が :16）。`v0.1.0` の公開はその **3 時間 7 分後**
      （07:45:30Z）であり、**リリースが 0 件の同じバッチで書かれた `v0.1.0` には
      公開が入っている。**
      **差は「配る予定が見えていたか」だけである。** `v0.1.0` は次に配る版だったので
      条件が「配ったこと」になり、**`v0.2.0` 以降は配る時期が見えていなかったので、
      description が機能の説明になった。** 08-24 に作った `v0.1.1` / `v0.1.2` は
      どちらも配る先が見えており、両方とも公開を含んでいる。
      ★ **したがって再発するのは遠い版である**——**いま `v0.5.0` を作れば、また中身だけの
      条件になる。**
      **中身だけを条件にすると、機能が揃った時点で条件は成立するのに関係のない issue が
      open で残る**——#83 が見つけた「`v0.2.0` は 5 本閉じれば条件を満たすのに 12 本残る」は
      これが原因である。**3 本の条件に公開を足し、6 本の形を揃えた**
- [x] ★ **訂正**——#84 で `v0.2.0` の description に入れた
      「**暗号化に関与しないものは載せない。次の版が持つ**」は**外した**（同上）。
      **`CLAUDE.md` の規約と正面から衝突していた。** `v0.1.2` を出した瞬間に
      `v0.2.0` が着手する版になり、規約は「そこへ付けろ」、description は「載せるな」と言う。
      **今回直したのと同じ状態が、規約に従った結果として再発する形だった。**
      ★ **description を「次の版が持て」と書いて塞ぐ方向そのものが誤りである**——
      時期が決まっているものは**次に配られる版に乗る**のだから、着手する版に付くのが正しい。
      再発を止めるのは「**版を前に挿したら、後ろの版の中身を見直す**」のほうである。
      起きたことの記録だけ description に残した
- [x] ★ **配る差分の門の 2 段目で、`ultra` の発火条件を書き直した**（#96、2026-08-30）。
      **無料は Pro / Max で 3 回きり・補充なし・アカウント単位**であり、**#93 で起動して
      初めて尽きていると分かった。** 以降はプランの枠では払えない（usage credits のみ。
      **画面の使用量はセッション 8% / 週間 16% で余っていたのに止まった**）。
      ★★ **捨てはしなかった。実走 2 回はどちらも効いている。**
      **#67** は `bug_001`（normal）——タグ付き PDF の `/StructTreeRoot` が削除したページの
      辞書を掴んだままで、3 ページから 1 ページを取り出すと **`SECRETA` / `SECRETB` が
      出力の trailer から辿れた。#46 が塞いだつもりの経路そのもの**であり、
      **Advisory を出す直前に捕まえている。** 2 件目（寄与しない入力への署名警告）も優先順位 2。
      **#87** も **CWE-377（symlink 置換）を塞ぐ修正**であり、出た 2 件のうち 1 件は
      **保存が成功しているのに「操作に失敗しました。」が出る**——これも優先順位 2 である。
      ★★ **したがって「効かない場面で撃っていた」わけではない。** 最初そう読んだが、
      **#87 を「nit 2 件」と要約したのが誤りだった**（`/code-review high` の指摘で気づいた）。
      外せるのは **配布物の振る舞いが変わらないもの**（#93 の統合 ／ #68 の文書）と、
      **配布物に入らないもの**（#69 の `tools/`）だけである。
      ★★ **v0.2.0 の 5 件は全部が当たる。絞ってもあの版のコストは下がらない。**
      **払うかどうかは別の判断であり、#96 では決めていない。**
      ★ **3 回目の消費は記録に無い。** 追えるのは #67 と #87 の 2 回だけである。
      無料枠はアカウント単位なので、**org の他のリポジトリの作業が pdfjig の門を止めうる。**
      だから使ったら額・件数・掛けた範囲を書く
- [x] ★ **差別化軸を「人が直せる面」に置いた**（#86、2026-08-30）。機能数では PDFsam /
      Stirling PDF / qpdf と重なっており、そこでは差別化が成立しない。
      ★★ **「承認の仕掛けが無いから作る」ではない。最初そう書いたが、実測で覆した。**
      **MCP の elicitation にも、ホストのツール許可ダイアログにも承認の仕掛けはあり、
      Foxit / Apryse の MCP サーバは後者を承認面として説明している。**
      **無いのは「中身を直せる面」のほうである**——elicitation の form モードは仕様が
      **「flat objects with primitive properties only」「complex nested structures,
      arrays of objects (beyond enums) ... are intentionally not supported」**と書いており、
      **しおりの木は原理的に載らない。** 仕様が用意した逃げ道（URL モード、2025-11-25 で追加）は
      **サーバがページを配ってブラウザで開かせる形**であり、**ポートを開けることを要求する。**
      ★ **推測ではなく機構で説明が立つので、訂正して主張が強くなった。**
- [x] **既存の PDF MCP サーバの調べ直し**（同上）。#86 の本文は「10 前後のうち 1 本を読んだ」
      だったが、**2026-08-30 に検索し直すと商用ベンダが既に入っていた**——Foxit（30+ ツール）／
      Apryse ／ pdf.net ／ pdfAssistant ／ Stirling PDF 用。
      ★★ **「手」は commodity どころか、20 年 PDF を書いてきたベンダの commodity である。**
      **したがって「賭けに負けても良いツール群が残る」という受け皿は無い。**
      受け皿は**デスクトップアプリ自身**——AI が 1 行も無くても完結する編集機能でなければならず、
      それが `SPEC.md` §8 の M1.5 を前半と後半に分けた理由である。**前半にだけ版を付けた。**
      なお**しおりを中身から生成するものは MCP には無い**が、**MCP の外には既にある**
      （tocPDF / pdfoutliner）——どれも**既にある目次ページを機械で読む**方式で、
      目次の無い文書とスキャンには効かず、承認面も無い
- [x] ★ **表抽出の再評価の結論は「縮まない」だった**（同上）。#86 の本文は「ページを見る AI は
      tabula より正確に読むので、tabula の役割は主たる抽出器から量を畳む道具へ縮む」と書いたが、
      **採らなかった。** tabula は**座標とセル構造**を決定的に返す。**モデルの出力は照合する先が
      無いので検算できない**——元がページの絵であり、突き合わせる相手がそもそも存在しない。
      **優先順位 1 の下で、検算できないものを主たる抽出器にはできない。**
      罫線のある表は tabula、罫線の無い表とスキャンはモデル。**競合ではなく住み分けである。**
      ★★ **帰結: POI を戻す判断はこの再評価に依存しない**（#32 / #14）。**依存するのは #31 の
      設計だけである。** #98 が「#86 が後ろの版を止めている」と読んだ範囲は、そのぶん狭い
- [x] **判断する側へ開く口の土台は、自前の JSON-RPC にする**（同上）。**2026-08-27 実測の SBOM
      増分は、MCP Java SDK が +13（`mcp-core` + `mcp-json-jackson3`）、自前が +0〜4。**
      **POI の +56 に比べれば SDK も軽く、重さを理由に却下する根拠は弱い。**
      それでも自前を採ったのは、**stdio の PDF サーバに YAML パーサと JSON Schema バリデータが
      要らない**からである——**同梱物に使わないものを混ぜると、本当に見るべきものが埋もれる**
      （Spotless を依存グラフに載せなかったのと同じ判断）。実装は `initialize` /
      `notifications/initialized` / `tools/list` / `tools/call` / `ping` だけで足りる。
      ★ **見直す契機**——**elicitation か sampling を使う日**（そこから先は仕様の版追従が
      自前の負担として効いてくる）／**版差で 2 回以上直した日**。
      **どちらも来ていないうちに SDK へ移らないこと**
- [x] **`jpackage --add-launcher` は現行の経路に載る**（同上、**2026-08-30 実測**。手元の
      JDK 21.0.8+9 で `jpackage --help`）。`--add-launcher <名前>=<プロパティファイル>` は
      `main-jar` / `main-class` / `java-options` / `win-console` / `win-shortcut` / `win-menu` を
      取る。`pdf-desktop/build.gradle.kts` の `jpackageAppImage` に足すだけでよく、
      **MSI と EXE は `--app-image` にそのアプリイメージを渡し、ZIP は同じものを
      固めるだけなので、3 種ともランチャがそのまま乗る。jlink ランタイムも共有される。
      配布系統は増えない。**（★ `packageZip` は jpackage を呼ばない Gradle の `Zip` である。
      **「3 種とも `--app-image` を渡している」ではない**）
      ★ **残る未確認が 1 つある**——stdio には `win-console=true`（コンソールサブシステム）が
      要り、**ホストが起動したときにコンソール窓が瞬く可能性がある。実装するときに実測すること**
- [x] **塗り潰しの下のテキスト検出は `extractWithPositions` では足りない**（同上、2026-08-30）。
      `PositionedText` が持つのは**グリフの座標だけ**で、**塗り潰し矩形（`re` + `f`）の座標を
      得る経路が無い。** `PDFGraphicsStreamEngine` で描画演算子を読む実装が別に要る。
      「原理的に可能」は変わらないが、**見積もりは 1 段上がる。**
      ★ **除去はしない**（本文の改変。INV-4 の表）。**見つけたが直せないと正直に言う側である**
- [x] **法務の区分は 4 のまま**（同上、2026-08-30 に org `legal-baseline.md` §1 で確認）。
      判断する側へ開くモードでは**当社の設備が何も受け取らず、鍵も持たない。**
      ★ **崩れるのは BYOK で鍵の所在が動いたときである**（`CLAUDE.md`「セキュリティ」）
- [x] ★ **Code scanning を入れた**（#23、2026-08-30）。**default setup。ワークフローは 1 本も足していない。**
      期限は「M1 の着手前」であり、**M1 は `v0.2.0` から始まるので、`v0.1.2` が最後の機会だった。**
      **枠を根拠に断れない**——public の標準ランナーは org の 2,000 分を消費しない
      （org `ci-strategy.md` §1、`security-verification.md` §0 の★★の 1 つ目）。
  - **advanced setup（ワークフローを置く形）を採らなかった。主たる理由はワークフローを
    1 本も増やさないことである**——org `ci-strategy.md` が「足す前に読む」と言う対象そのものが
    増えない。設定は API 1 回で済み、SHA ピンも `timeout-minutes` も `permissions` も要らない。
    ★ **副次的に、org `security-verification.md` §3 が「default setup を選ぶなら
    SARIF の POST（`/code-scanning/sarifs`）を経由しない」と書いている**ので、
    あちらが未実走として残している経路を踏まない側にはなる。
    **ただし内部で同じ経路を通るかどうかは確かめていない。**
    **今回の実走で分かったのは「default setup では alert がちゃんと出る」ことまでである**
    ——この根拠に寄りかからないこと。
    `paths-ignore` が使えず**文書だけの PR でも走る**のが代償だが、
    **絞る根拠（枠）が public では効かない**ので、残るのは待ち時間だけである
  - ★★ **`threat_model` を `remote_and_local` にした。既定の `remote` では足りない。**
    CodeQL の既定はネットワーク入力を汚染源とみなすが、**この道具の脅威は手元のディスクから
    開く細工 PDF である**（`SECURITY.md`「対象範囲」がそう書いている）。
    **`remote` のままだとローカルのファイルが汚染源にならず、対象範囲に書いてあるものを
    ちょうど見逃す。** 分類 P で既定をそのまま採らない実例
  - **実測（2026-08-30、初回 run 33288121015）** — `Analyze (actions)` **40 秒**、
    `Analyze (java-kotlin)` **2 分 20 秒**。**`build` は直近 6 本で 3 分 41 秒〜6 分 38 秒**
    （2026-08-28〜30 実測）なので、**いちばん速い `build` よりまだ短い。**
    `languages` は `[actions, java-kotlin]` で、**ワークフローの側も見られている**。
    ★ **`schedule` は `weekly` が入った**（PATCH の直後は `null` に見えるので、後から読み直すこと）
  - **出た alert は 5 件。全部 `java/path-injection` で、全部 `tools/`。**
    **`pdf-core` / `pdf-desktop` / `pdf-cli` は 0 件である。**
    ★ **却下した**（`won't fix`）——**`tools/` は配布物に入らない**（#69 の軸）うえ、
    汚染源は**開発者自身が打つ `main` の `args[0]`** で、出力先を決めるのも同じ開発者である。
    `CLAUDE.md` セキュリティ節の §3.3 の読み替えがそのまま当たる。
    ★★ **この軸を配布物へ持ち込まないこと。** 却下の理由は各 alert のコメントにも書いてある
  - **Copilot Autofix が効き始めた**（それまでは On のまま何もしていなかった）。
    **提案を中身を読まずにマージしない**を `CLAUDE.md` のセキュリティ節に書いた。
    ★ **org `security-baseline.md` §5.2 の全体が効き始めたわけではない**——
    あちらは pdfjig 自身が AI を組み込む日の話で、**そちらは #80 が持つ**（`v0.5.0`）。
    今回当たったのは「**モデルの出力を信頼された入力として扱わない**」の 1 行だけで、
    **対象は製品ではなく開発の道具である**
  - **#22 の Check runs failure threshold は、これで対象になる check run が生まれた。
    ただし空振りは終わっていない。** ruleset 21228747 は `deletion` と `non_fast_forward` の
    2 つのままで、**閾値そのものを設定していない。**
    ★★ **そして `develop` には rule が 1 本も無い**（`rules/branches/develop` が `[]`。
    2026-08-30 実測）。**High の alert が出ても、いま何も止まらない。**
    **無くなったのは 2 つある原因のうちの 1 つだけである**
  - ★ **org 正本の実測表が、この変更で古くなった。** `security-verification.md` §3 は
    pdfjig について `alerts → 404 no analysis found` ／ `default-setup → state: not-configured` と
    書いているが、**どちらも今日から偽である。**
    **`propagandist/.github#103` へ起票した。ここへ写さない**——
    #23 のコメントが使った順序（**org → pdfjig**）と同じである
- [x] ★ **設定の置き場を `%LOCALAPPDATA%\pdfjig\` に決めた**（#12、2026-08-30）。
      **issue の本文は `%APPDATA%` を想定していたが、採らなかった。**
      **覚えるのはパスである。** ローミングプロファイルは別のマシンへ運ばれるので、
      **`D:\scan\` を運べば必ず外れる値を運ぶことになる。**
      ローミングは容量に上限を掛けられる運用も多く、**ログを同じ木に置く**（#13）**とそこに効く。**
      EXE の入れ先も `--win-per-user-install` で `%LOCALAPPDATA%` であり、**入れ物と持ち物が揃う。**
      **木の形と、何を置き何を置かないかは `docs/SPEC.md` §10 が持つ。ここへ写さない。**
  - **形式は `Properties` の text（UTF-8）。** JDK だけで足り、**依存も jlink のモジュールも増えない。**
    JSON にすると `pdf-desktop` に構文解析が要る。**人が開いて読めること**を優先した——
    **何を覚えているかを利用者が確かめられる状態を保つ**（優先順位 2）。
    ★ **`store` は `\` と `:` をエスケープする**ので、`C:\scan` はファイルの中で `C\:\\scan` と見える。
    **読み書きが対なので壊れないが、手で直すときに驚く。** 往復のテストで縛ってある
  - ★ **読めなければ空、書けなければ諦める。例外を投げない。**
    覚えているのは次にダイアログが開くフォルダだけで、失われても選び直せば済む。
    **設定が壊れているせいでアプリが起動しない形を作らない。**
    ★★ **ここが #13 との接点である**——**捨てたことも書けなかったことも、いまは誰にも伝わらない。**
    ログの仕組みが入ったらここに 1 行足す。**#98 が #12 → #13 の順にした理由がここに出ている**
  - **書くのは終了時に一度だけ**（`setOnHidden`）。操作のたびに書くと、覚えているのが
    フォルダ 2 つだけのために I/O が増える。**異常終了すると失われる**が、選び直せば済む
  - **書き方は一時ファイル → 置き換え**（#53 と同じ判断）。名前だけ先に決めて直接書くと、
    書いている途中で終了したときに壊れたファイルが残る。
    `ATOMIC_MOVE` が断られるファイルシステムでは普通の置き換えに落とす——
    **書き終えた一時ファイルからの置き換えなので、落としても壊れたファイルにはならない**
  - ★ **設定の読み書きは `PdfjigApplication` が持つ。** `MainWindow` は `folders()` を 1 つ出すだけで、
    **置き場も書き方も知らない**——画面がファイルの読み書きを抱えると #57 が重くなる。
    ★ **副作用として、`uiTest` が実際の設定ファイルに触れない**（あちらは `MainWindow` を直に組む）。
    **開発機の `%LOCALAPPDATA%` をテストが書き換える形にならない**
  - ★★ **`build` は `uiTest` をコンパイルしない。** `uiTest` は独自のソースセットで、
    `compileUiTestJava` は `assemble` にも `check` にも載っていない。
    **画面まわりの署名を変えたら `./gradlew :pdf-desktop:compileUiTestJava` を明示的に回すこと**
    ——**緑のまま壊れる。** 今回は `folders()` を足しただけなので当たらなかったが、
    構築子を変えていれば当たっていた
  - **`LOCALAPPDATA` が無い環境では何も置かず何も読まない**（Windows 以外。**CI の ubuntu もここ**）。
    **覚えられないことは、動かない理由ではない**
  - **INV-5** — 書き出されるものが**渡した鍵だけ**であることをテストで縛った。
    パスワードを置く API はそもそも無いが、**後から `putAll` のような口が開くと平文のまま残る**
- [x] ★ **`ultra` の条件を、初めて境界の差分に当てた**（#107、2026-08-30）。**掛けないと決めた。**
      条件は「**配布物の振る舞いが変わり、かつ利用者のデータを扱う**」（#96 / #97）。
      #107 は**前半に当たり、後半に当たらない**——**利用者の PC に新しい書き込み経路を作るが、
      書くのはダイアログのフォルダであって PDF ではなく、文書の読み書き経路は 1 行も触っていない。**
  - ★ **迷う材料はあった。** #87（一時ファイルの作り方を変えただけ）が **CWE-377** を出しており、
    **書き込み経路の安全性という型は同じ**である。**判断は人が下した**（無料枠は尽きており、
    起動と支払いは人にしかできない。#96）
  - ★★ **したがって線はここに引かれた**——**新しい書き込み経路を作っても、
    そこを通るのが利用者の文書でなければ掛けない。** 次に同じ形が来たらこれに揃える
  - ★★ **#98 の門は「残り 10 件のうち `ultra` に当たりうるのは #57 だけ」と読んでいた。外れである。**
    **#12 の実装も条件の前半に当たった。**「当たりうるものを事前に数え上げる」形が
    うまくいかないことの実例として残す
  - **掛けなかったが、`/code-review high` は実際の欠陥を 3 件出している**
    （自クラスの「例外を投げない」契約を破る NPE ／ `dispose` が飛ばされて元の PDF を掴み続ける ／
    JavaFX スレッドでの `isDirectory`）。**必須の 2 本が効いていないわけではない**
- [x] ★ **ログの出力先とローテーションを決めた**（#13、2026-08-30）。
      **`%LOCALAPPDATA%\pdfjig\logs\` に 512 KB × 3 世代。`WARNING` 以上だけを書く。**
      **決め手は「配布物には標準エラー出力が無い」ことである**——`jpackage` は `--win-console` を
      付けずに GUI アプリとして固めるので、**異常終了しても手がかりが 1 つも残らない。**
      **木の形と、何を書き何を書かないかは `docs/SPEC.md` §10.4 が持つ。ここへ写さない。**
  - ★★ **文書のパスもファイル名も書かないと決めた。** §10.1 の「置かない」表は既に
    「開いていた文書のパス」を挙げており、**ログに書けば理由は違えど同じものが残る。**
    ログはアンインストールでも消えない（§10.3）ので、**業務文書の一覧という新しい記録を
    作ることになる。** 報告のために貼る人が伏せ字にしなくて済む、という利点も同じ側にある
  - ★★ **例外のメッセージも書かない。** `NoSuchFileException` のメッセージは**パスそのもの**で、
    **`SimpleFormatter` は `printStackTrace` を通すのでそこが素通りする。**
    独自の `Formatter` が要るのはこのためである。**スタックフレームは書く**——
    `StackTraceElement` が持つのはクラス名・メソッド名・ソースファイル名・行番号だけで、
    利用者のファイルの名前は入らない
  - ★ **守り方を 2 段にした。** 口が `LogEvent`（列挙）しか受け取らないので**呼ぶ側が
    パスを載せられない**。そして **`java.util.logging` を直に触ることを ArchUnit が禁じる**
    ので、**口を迂回できない。** 型で守っているものを、型を迂回して破れないようにしてある
    （`loggingMustGoThroughLogs`。**迂回する差分で実際に赤になることを確かめた**）
  - **実装は `java.util.logging`。** 依存も jlink のモジュールも増えない——`java.logging` は
    既にルートモジュールに入っている（`pdf-desktop/build.gradle.kts`）。#12 と同じ線である
  - ★ **最初の 1 件が出るまで開かない。** 正常に動いている限り `logs\` フォルダそのものができない。
    **中身の無いファイルを置かない**のは #12 の判断をそのまま延ばしたものである
  - ★★ **1 つの例外について書くフレームに上限（40）を置いた。** 置かないと
    **`StackOverflowError` 1 件が世代を丸ごと押し出す**——1000 を超えるフレームを持ち、
    **まさに捕まえられなかった例外として来る種類である。** 押し出されるのは、
    その直前に起きた**本当の原因**のほうである
  - **握り潰していた 7 箇所をここへつないだ**（設定の読み・書き・値、作業場所の後始末 3 箇所、
    画面に出した失敗）。★ **サムネイルの描画失敗はつないでいない**——
    壊れた文書ではページ数ぶん出て、**他の記録を押し出す。** 画面には既に理由が出ている
  - ★ **終了時に閉じない。** `LogManager` の停止フックが閉じるので、
    **閉じる途中で落ちたものを取り逃がさない**ほうを採った
  - ★ **`%LOCALAPPDATA%` が無い環境では何もしない**（#12 と同じ）。
    **`FileHandler` のパターンは `%` を特別扱いする**ので、置き場のパスに入った `%` は
    `%%` へ逃がす——`C:\...\100% 完了\` の下では違う場所へ書きに行く（テストで縛った）
