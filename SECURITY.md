# セキュリティポリシー

pdfjig は利用者の PDF を、その利用者の手元で開き、書き換え、書き出す。
そのため脆弱性の影響が**利用者の機密文書に直接及ぶ**。

**経路はパスワードと暗号化だけではない。** この道具の仕事は「綴じる・解く・取り出す」ことであり、
**利用者は取り除いた結果を配ることを前提に使う。** 取り除いたはずのものが出力に残れば、
攻撃者が一人も居なくても機密文書が渡る。報告は歓迎する。

## 報告方法

**公開の Issue や Pull Request に書かないこと。** 修正前に詳細が公開されると利用者が危険に晒される。

いずれかの方法で連絡してほしい。

1. **GitHub Security Advisories（推奨）**
   リポジトリの [Security] タブ → [Report a vulnerability] から非公開で報告できる。
2. **問合せフォーム**
   https://www.propagandist.co.jp/contact/

報告に含めてほしい情報:

- 影響を受けるバージョン
- 再現手順
- 想定される影響範囲

**再現用の PDF に実業務の書類を使わないこと。** 添付は最小限の自作サンプルに留めてほしい。
問合せフォームはファイルを添付できない。再現用ファイルを送る必要がある場合は GitHub Security Advisories を使ってほしい。

## 対応方針

報告を受け取ったら、**1 週間以内**を目安に受領の返信をする。
それを過ぎても返信が無い場合は届いていない可能性があるので、再送してほしい。

評価と修正に要する期間は、深刻度と影響範囲によって変わる。少人数で開発しているため、
時期によっては時間がかかる。**進展が無い場合も、状況は定期的に伝える。**

公表の時期は報告者と調整する。修正版のリリースをもって Security Advisory を公開し、
希望があれば報告者を明記する。**報告者が期限を切って公表することを妨げない。**

## 修正版の届き方

pdfjig は利用者の PC にインストールされて動く。**自動更新の仕組みを持たない。**
修正版をリリースしても、利用者が入れ替えない限り古い版が手元に残る。

修正は GitHub Releases に出し、あわせて Security Advisory を公開する。
入れ替えは利用者の操作に依るため、Releases の Watch を勧める。

## 対象範囲

以下は脆弱性として扱う。

- パスワード・平文の鍵素材がログ・例外メッセージ・設定ファイル・スタックトレースに漏出する
- 暗号化された入力から、利用者の意図に反して平文の出力が生成される
- 細工された PDF によって任意コード実行・想定外のファイル書き込みが発生する
- **pdfjig が** AI プロバイダに対し、抽出テキスト以外（PDF バイナリ・パスワード等）を送信する。
  **判断する側の道具が自分で PDF を読むことは含まない**——読ませているのは利用者であり、
  pdfjig の経路ではない（`docs/SPEC.md` §5.1 / §5.4）
- **利用者が取り除いた・消したはずの内容が出力に残り、ファイルの中から辿れる。**
  画面や API の上では消えたように見えるのに、出力を開いて参照を辿ると出てくる状態を指す。
  **ページの除去に限らない**——文書情報・メタデータ・注釈・添付など、
  「消したつもりのもの」すべてが射程に入る。
  枚数にもページ一覧にも出ないため、**渡す側には確かめる手段が無い**
- **AI が提案した操作が、人の承認を経ずにファイルへ適用される。**
  細工された PDF の本文が判断する側のコンテキストに入る経路そのものは塞げない——
  読ませるために渡すのだから。**したがって守りは検出ではなく構造に置いてある。**
  どの口も削除・上書きをせず、**書き込みは必ず、人が中身を見て直せる面を通る**
  （`docs/SPEC.md` §1 の 5）。**この構造が破れている状態**を指す
- **利用者が開いていない文書の中身が、道具の戻り値として外へ出る。**
  複数のファイルを横断して探す操作が、一致したテキストまで返す状態を指す。
  **返すのはパスとヒット件数まで**である（同 §5.4）

以下は**脆弱性として扱わない**。

- **権限フラグ（印刷禁止・コピー禁止）が回避できること。**
  PDF 仕様上、ユーザーパスワードが空の場合これらは暗号学的に強制されず、閲覧ソフトの
  自主的な遵守に依存する。仕様であって不備ではない。
  ただし**暗号化を設定する画面でこの点を明示することは要件として決めてある**
  （`docs/SPEC.md` §6.1）。**明示が無いまま設定できてしまう状態は、こちらの不備として扱う。**
- 依存ライブラリの既知の脆弱性のうち、pdfjig の利用経路から到達しないもの（報告は歓迎するが優先度は下がる）

## サポート対象バージョン

**最新のリリースのみ**を対象とする。過去の版へ遡って修正版は出さない。
