# セキュリティポリシー

pdfjig は PDF のパスワード・暗号化を扱う。そのため脆弱性の影響が利用者の機密文書に直接及ぶ可能性がある。
報告は歓迎する。

## 報告方法

**公開の Issue や Pull Request に書かないこと。** 修正前に詳細が公開されると利用者が危険に晒される。

いずれかの方法で連絡してほしい。

1. **GitHub Security Advisories（推奨）**
   リポジトリの [Security] タブ → [Report a vulnerability] から非公開で報告できる。
2. **問合せフォーム**
   https://www.propagandist.co.jp/contact/

報告に含めてほしい情報:

- 影響を受けるバージョン
- 再現手順
- 想定される影響範囲

**再現用の PDF に実業務の書類を使わないこと。** 添付は最小限の自作サンプルに留めてほしい。

## 対応方針

| 段階 | 目安 |
|---|---|
| 受領確認の返信 | 3 営業日以内 |
| 影響範囲の一次評価 | 14 日以内 |
| 修正版のリリース | 深刻度に応じて調整。目安は 90 日以内 |

公表のタイミングは報告者と調整する。修正版のリリースをもって Security Advisory を公開し、希望があれば報告者を明記する。

## 対象範囲

以下は脆弱性として扱う。

- パスワード・平文の鍵素材がログ・例外メッセージ・設定ファイル・スタックトレースに漏出する
- 暗号化された入力から、利用者の意図に反して平文の出力が生成される
- 細工された PDF によって任意コード実行・想定外のファイル書き込みが発生する
- AI プロバイダに対し、抽出テキスト以外（PDF バイナリ・パスワード等）が送信される

以下は**脆弱性として扱わない**。

- **権限フラグ（印刷禁止・コピー禁止）が回避できること。**
  PDF 仕様上、ユーザーパスワードが空の場合これらは暗号学的に強制されず、閲覧ソフトの自主的な遵守に依存する。
  pdfjig はこの点を UI 上で明示している。仕様であって不備ではない。
- 依存ライブラリの既知の脆弱性のうち、pdfjig の利用経路から到達しないもの（報告は歓迎するが優先度は下がる）

## サポート対象バージョン

最新のリリースのみを対象とする。
